---
phase: 32-executive-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/exports/pdf.ts
  - src/exports/pdf-templates.js
  - src/exports/pdf-engine.js
  - scripts/validate-reporting.js
  - scripts/validate-i18n.js
autonomous: true
requirements: [UI-06]
must_haves:
  truths:
    - "Executive PDF renders as a professional corporate report with a stable, predictable section sequence."
    - "Portuguese and Spanish accented characters render correctly in executive PDF output with no replacement glyph regressions."
    - "Executive PDF uses the same executive read model contract as the web executive page for equivalent filters and principal."
  artifacts:
    - path: "src/exports/pdf.ts"
      provides: "Executive PDF assembly that formats output directly from buildExecutiveSummary(filters, principal)"
    - path: "src/exports/pdf-templates.js"
      provides: "Deterministic executive report section structure and professional report HTML semantics"
    - path: "src/exports/pdf-engine.js"
      provides: "Unicode-safe PDF byte generation path consumed by executive export"
    - path: "scripts/validate-reporting.js"
      provides: "Automated anchors for fixed executive section sequence and read-model parity usage"
    - path: "scripts/validate-i18n.js"
      provides: "Automated Unicode/accent regression checks for executive PDF export text path"
  key_links:
    - from: "src/app/reporting/export/executive.pdf/route.ts"
      to: "src/exports/pdf.ts"
      via: "buildExecutivePdf(filters, principal)"
      pattern: "buildExecutivePdf\\(filters, principal\\)"
    - from: "src/exports/pdf.ts"
      to: "src/reporting/read-models.ts"
      via: "shared executive read model"
      pattern: "buildExecutiveSummary\\(filters, principal\\)"
    - from: "src/exports/pdf.ts"
      to: "src/exports/pdf-templates.js"
      via: "renderExecutiveReportHtml executive section contract"
      pattern: "renderExecutiveReportHtml"
---

<objective>
Deliver UI-06 by making executive PDF output professional, Unicode-safe, section-order deterministic, and contract-aligned with the web executive read model.

Purpose: Satisfy locked decisions D-01 through D-04 without introducing a parallel export data pipeline.
Output: Updated executive PDF renderer/template/validators with deterministic verification gates for section order and accent-safe output.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-executive-pdf/32-CONTEXT.md
@.planning/phases/32-executive-pdf/32-RESEARCH.md
@src/reporting/read-models.ts
@src/app/reporting/executive/page.tsx
@src/app/reporting/export/executive.pdf/route.ts
@src/exports/pdf.ts
@src/exports/pdf-templates.js
@src/exports/pdf-engine.js
@scripts/validate-reporting.js
@scripts/validate-i18n.js

<interfaces>
From src/reporting/read-models.ts:
```typescript
export interface ExecutiveSummary { /* totalRows, topRisks, trend, breakdowns, derivedSourceHealth, ... */ }
export async function buildExecutiveSummary(
  filters: ReportFilters,
  principal?: AuthenticatedPrincipal
): Promise<ExecutiveSummary>;
```

From src/exports/pdf.ts:
```typescript
export async function buildExecutivePdf(
  filters: ReportFilters,
  principal?: AuthenticatedPrincipal
): Promise<PdfExportResult>;
```

From src/app/reporting/export/executive.pdf/route.ts:
```typescript
const result = await buildExecutivePdf(filters, principal);
return new Response(result.bytes, { headers: { "content-type": "application/pdf" } });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce executive read-model parity and Unicode-safe PDF rendering path</name>
  <read_first>
    - .planning/phases/32-executive-pdf/32-CONTEXT.md
    - src/exports/pdf.ts
    - src/exports/pdf-engine.js
    - src/reporting/read-models.ts
    - src/app/reporting/executive/page.tsx
  </read_first>
  <files>src/exports/pdf.ts, src/exports/pdf-engine.js</files>
  <acceptance_criteria>
    - Implements D-04 by keeping `buildExecutivePdf()` as a formatter over `buildExecutiveSummary(filters, principal)` fields only.
    - Implements D-03 by replacing the current fragile text-stream PDF path with a Unicode-safe byte generation strategy for executive exports.
    - Preserves existing export route/auth contract and `application/pdf` response behavior.
  </acceptance_criteria>
  <action>Refactor executive export rendering internals so the produced PDF bytes support accented/Unicode content reliably, while preserving the existing route contract and principal-scoped filter behavior. Keep `buildExecutivePdf()` sourced from `buildExecutiveSummary(filters, principal)` exactly (no parallel aggregation logic), and keep operational PDF behavior compatible unless intentionally updated with equivalent safety guarantees.</action>
  <verify>
    <automated>node scripts/validate-reporting.js executive && node scripts/validate-reporting.js pdf-bytes && npm run typecheck</automated>
  </verify>
  <done>Executive PDF rendering path is Unicode-safe and still driven directly by the shared executive read model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fixed professional executive section contract</name>
  <read_first>
    - .planning/phases/32-executive-pdf/32-CONTEXT.md
    - src/exports/pdf-templates.js
    - src/exports/pdf.ts
    - src/app/reporting/executive/page.tsx
  </read_first>
  <files>src/exports/pdf-templates.js, src/exports/pdf.ts</files>
  <acceptance_criteria>
    - Implements D-01 with classic corporate visual structure: sober headings, clear metric tables/lists, restrained decoration.
    - Implements D-02 with this exact section order: cover/scope -> executive summary -> derived source status -> top risks -> trend -> final notes.
    - Uses deterministic section labels/content wiring so exported reports remain predictable across runs.
  </acceptance_criteria>
  <action>Restructure executive template composition and executive label wiring to encode the required fixed order and professional layout semantics. Ensure derived-source status is represented as its own explicit section in the sequence, and add a final notes section with executive-safe wording while keeping content sourced from existing summary fields and localization labels.</action>
  <verify>
    <automated>node scripts/validate-reporting.js pdf-audit && node scripts/validate-reporting.js pdf-routes</automated>
  </verify>
  <done>Executive PDF template consistently renders the mandated section order and professional report composition.</done>
</task>

<task type="auto">
  <name>Task 3: Add hard release gates for section order and accent-safe output</name>
  <read_first>
    - scripts/validate-reporting.js
    - scripts/validate-i18n.js
    - src/exports/pdf.ts
    - src/exports/pdf-templates.js
    - src/exports/pdf-engine.js
  </read_first>
  <files>scripts/validate-reporting.js, scripts/validate-i18n.js</files>
  <acceptance_criteria>
    - D-02 is enforced by deterministic automated checks for executive section sequence anchors.
    - D-03 is enforced by automated PDF-path checks containing accented/Unicode fixture text.
    - Validation commands fail fast if parity/ordering/unicode expectations regress.
  </acceptance_criteria>
  <action>Extend reporting and i18n validators with explicit executive-PDF assertions: required section anchor order, presence of derived-source and final-notes sections, and Unicode/accent fixture survival through the executive PDF text extraction path. Keep validations script-driven and deterministic so they can act as release gates for UI-06.</action>
  <verify>
    <automated>node scripts/validate-i18n.js exports && node scripts/validate-reporting.js pdf-audit && npm run validate</automated>
  </verify>
  <done>UI-06 regression checks are automated and block release when section order or Unicode behavior degrades.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> executive export route | Untrusted query/filter input enters PDF export pipeline |
| read model -> PDF template rendering | Data crosses from typed model into rendered report output |
| rendered HTML/text -> PDF bytes | Encoding and glyph handling boundary where Unicode corruption can occur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | I | executive.pdf route | mitigate | Keep `assertAuthenticated()` and principal-scoped `buildExecutivePdf(filters, principal)` path unchanged. |
| T-32-02 | T | pdf template composition | mitigate | Keep template values escaped/safe and sourced from typed summary fields; avoid raw untrusted HTML injection. |
| T-32-03 | D | pdf rendering engine | mitigate | Add deterministic validator gates for Unicode/accent fixtures and fail release on rendering regression. |
| T-32-04 | R | executive section contract | mitigate | Enforce fixed section-order anchors in validator scripts to prevent silent ordering drift. |
</threat_model>

<verification>
Run:
- `node scripts/validate-reporting.js executive`
- `node scripts/validate-reporting.js pdf-bytes`
- `node scripts/validate-reporting.js pdf-routes`
- `node scripts/validate-reporting.js pdf-audit`
- `node scripts/validate-i18n.js exports`
- `npm run typecheck`
- `npm run validate`
</verification>

<success_criteria>
- UI-06 is fully delivered with D-01..D-04 traceable in implementation and validators.
- Executive PDF appearance is professional and section order is fixed/predictable.
- Unicode/accent regressions are blocked by automated checks.
- Web executive view and PDF export remain aligned through one shared read model path.
</success_criteria>

<output>
After completion, create `.planning/phases/32-executive-pdf/32-01-SUMMARY.md`
</output>
