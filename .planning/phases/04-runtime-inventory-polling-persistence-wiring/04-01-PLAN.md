---
phase: 04-runtime-inventory-polling-persistence-wiring
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified: [src/inventory/targets.ts, src/inventory/target-admin.ts, src/polling/scheduler.ts, src/validation/lcr-validator.ts, src/alerting/alert-policy.ts, src/storage/coverage-records.ts, src/storage/runtime-store.ts, scripts/validate-runtime-wiring.js]
autonomous: true
requirements: [MON-01, CFG-01, INT-01, ALT-01]
user_setup: []
must_haves:
  truths:
    - "Inventory must load real targets from the database source of truth instead of a placeholder array."
    - "Operators must be able to add or update inventory records through a non-code path backed by the database."
    - "The scheduler must call validation, persist poll/validation/snapshot evidence, and persist alert events before attempting delivery."
    - "Runtime evidence must survive restarts and expose minimal read contracts for downstream reporting work."
  artifacts:
    - src/inventory/targets.ts
    - src/inventory/target-admin.ts
    - src/polling/scheduler.ts
    - src/storage/runtime-store.ts
    - src/storage/coverage-records.ts
    - src/alerting/alert-policy.ts
  key_links:
    - src/inventory/target-admin.ts -> src/storage/runtime-store.ts
    - src/polling/scheduler.ts -> src/inventory/targets.ts
    - src/polling/scheduler.ts -> src/validation/lcr-validator.ts
    - src/polling/scheduler.ts -> src/storage/coverage-records.ts
    - src/polling/scheduler.ts -> src/alerting/alert-policy.ts
    - src/storage/coverage-records.ts -> src/storage/runtime-store.ts
---
<objective>
Close the runtime wiring gaps identified by the milestone audit by replacing placeholder inventory loading and in-memory event handling with a persistent monitoring pipeline.
Purpose: Phase 4 must make monitoring, validation, alert persistence, and runtime evidence actually work end-to-end before Phase 5 wires the reporting UX to this data.
Output: Database-backed inventory loading, scheduler-to-validation/alerting runtime flow, durable persistence/read contracts, and runtime wiring validation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder inventory loading with persistent runtime targets</name>
  <files>src/inventory/targets.ts, src/inventory/target-admin.ts, src/storage/runtime-store.ts, scripts/validate-runtime-wiring.js</files>
  <read_first>.planning/phases/04-runtime-inventory-polling-persistence-wiring/04-CONTEXT.md, .planning/phases/04-runtime-inventory-polling-persistence-wiring/04-RESEARCH.md, src/inventory/targets.ts</read_first>
  <action>Create `src/storage/runtime-store.ts` as a production-grade database-backed persistence adapter for runtime data, including inventory target records. Update `src/inventory/targets.ts` so `loadTargets()` reads normalized targets from the database source of truth rather than `DEFAULT_TARGETS`, while preserving interval, timeout, alert routing, retention, and enablement metadata. Add `src/inventory/target-admin.ts` with a minimal non-code write path for operators to add or update inventory records in the database without editing source files. Create `scripts/validate-runtime-wiring.js` in this task so the verification commands used by later tasks already exist.</action>
  <verify>`node scripts/validate-runtime-wiring.js inventory` exits 0 and prints `Runtime inventory ready`.</verify>
  <acceptance_criteria>
    - `loadTargets()` no longer depends on `DEFAULT_TARGETS` as the runtime source of truth.
    - Runtime target records are loaded from a database-backed source of truth and include interval, timeout, alert routing, and retention settings.
    - A non-code write path exists for operators to add or update target records in the database.
    - `scripts/validate-runtime-wiring.js` exists after this task and validates inventory/runtime readiness.
  </acceptance_criteria>
  <done>Scheduler can load real persisted targets instead of an empty placeholder set.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scheduler, validation, persistence, and alert recording</name>
  <files>src/polling/scheduler.ts, src/validation/lcr-validator.ts, src/alerting/alert-policy.ts, src/storage/coverage-records.ts, src/storage/runtime-store.ts</files>
  <read_first>.planning/phases/04-runtime-inventory-polling-persistence-wiring/04-CONTEXT.md, .planning/v1.0-MILESTONE-AUDIT.md, src/polling/scheduler.ts, src/validation/lcr-validator.ts, src/alerting/alert-policy.ts, src/storage/coverage-records.ts</read_first>
  <action>Update `src/polling/scheduler.ts` so the polling flow is explicit: load target from the database-backed inventory source, fetch artifact, validate artifact, persist poll + validation evidence + snapshot metadata, persist alert events before attempting delivery, and only then trigger alert delivery side effects. Update storage/alert modules as needed so invalid artifacts are still persisted for audit, duplicate hashes do not create duplicate blobs, and alert records survive restart through the production-grade database-backed persistence path rather than living only in memory.</action>
  <verify>`node scripts/validate-runtime-wiring.js scheduler` exits 0 and prints `Runtime scheduler wiring ready`.</verify>
  <acceptance_criteria>
    - Scheduler calls `validateLcr` during runtime handling of fetched artifacts.
    - Invalid artifacts are persisted for audit in the durable database-backed store and mark coverage unavailable.
    - Duplicate hashes record a verification/event without storing a duplicate blob.
    - Alert events are persisted before delivery is attempted.
  </acceptance_criteria>
  <done>Monitoring, validation, persistence, and alert recording are wired into one runtime path.</done>
</task>

<task type="auto">
  <name>Task 3: Deliver durable evidence model and minimal read contracts</name>
  <files>src/storage/coverage-records.ts, src/storage/runtime-store.ts, scripts/validate-runtime-wiring.js</files>
  <read_first>.planning/phases/04-runtime-inventory-polling-persistence-wiring/04-CONTEXT.md, .planning/phases/03-reporting-compliance-governance/03-CONTEXT.md, src/storage/coverage-records.ts</read_first>
  <action>Refactor `src/storage/coverage-records.ts` and supporting database-backed persistence utilities so polls, validation records, alerts, coverage gaps, and snapshots/blobs are durably represented and readable after restart. Expose minimal read contracts/adapters that Phase 5 can consume without redesigning the persistence model. Extend `scripts/validate-runtime-wiring.js` to verify inventory source replacement, runtime scheduler wiring, and durable read-contract availability.</action>
  <verify>`node scripts/validate-runtime-wiring.js persistence` exits 0 and prints `Runtime persistence ready`.</verify>
  <acceptance_criteria>
    - Runtime evidence is no longer represented only by module-local arrays.
    - Coverage gaps, poll rows, alert records, validation events, and snapshot metadata have durable read/write handling.
    - Minimal read contracts exist for downstream reporting consumers without introducing Phase 5 UI concerns.
  </acceptance_criteria>
  <done>Phase 5 has a stable persisted runtime data source to consume.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-runtime-wiring.js inventory` exits 0
- [ ] `node scripts/validate-runtime-wiring.js scheduler` exits 0
- [ ] `node scripts/validate-runtime-wiring.js persistence` exits 0
- [ ] `loadTargets()` is backed by persistence rather than placeholder defaults
- [ ] Scheduler invokes validation and persists alert events before delivery
- [ ] Runtime read contracts expose durable polls, alerts, gaps, and snapshots
</verification>

<success_criteria>
- Inventory loading and operator write-path are backed by the database/runtime store
- Scheduler/validation/alerting flow is wired end-to-end
- Runtime evidence is durable and ready for Phase 5 consumption
</success_criteria>

<output>
After completion, create `.planning/phases/04-runtime-inventory-polling-persistence-wiring/04-01-SUMMARY.md`
</output>
