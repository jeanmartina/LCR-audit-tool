---
phase: 13-real-oauth-and-oidc-authentication-delivery
plan: 01
type: execute
wave: 1
depends_on: [12-01]
files_modified: [src/auth/, src/app/api/auth/, src/app/auth/, src/app/settings/, src/storage/runtime-store.ts, scripts/, .planning/]
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04, AUTH-05]
user_setup:
  - provide real provider credentials and a public HTTPS host when executing the real end-to-end provider verification path
must_haves:
  truths:
    - "Invitation remains the gate for provider authentication."
    - "Google, Microsoft Entra ID, and generic OIDC must use real authorization-code-based redirect/callback flows."
    - "Invite acceptance happens only after provider identity is verified and the provider email matches the invited email."
    - "Provider secrets stay in environment variables and are never editable or visible in the UI."
    - "AUTH-05 is only closed by a real public-HTTPS callback proof path, not localhost-only testing."
  artifacts:
    - src/auth/
    - src/app/api/auth/
    - src/app/auth/
    - src/app/settings/
    - src/storage/runtime-store.ts
    - scripts/
  key_links:
    - .planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-CONTEXT.md
    - .planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-RESEARCH.md
    - .planning/v1.1-MILESTONE-AUDIT.md
    - .planning/REQUIREMENTS.md
---
<objective>
Replace the current provider-auth scaffolding with real invite-gated Google, Microsoft Entra ID, and generic OIDC flows that work over the packaged HTTPS deployment path.
Purpose: Phase 8 created the auth foundation and Phase 12 created the HTTPS runtime, but the milestone audit found that provider auth is still placeholder-only. Phase 13 closes that gap with real redirect/callback flows, strict identity-linking rules, and platform-admin provider-status visibility.
Output: real provider auth routes and state handling, verified callback/token validation path, platform-admin provider status surface, and validation coverage that proves the milestone blocker is closed.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder provider auth with real redirect and callback flows</name>
  <files>src/app/api/auth/, src/auth/, src/storage/runtime-store.ts</files>
  <read_first>.planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-CONTEXT.md, .planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-RESEARCH.md, src/app/api/auth/callback/[provider]/route.ts, src/auth/config.ts, src/auth/providers.ts, src/auth/invitations.ts, src/auth/models.ts, src/auth/session.ts</read_first>
  <action>Add the real provider-auth server flow for Google, Microsoft Entra ID, and generic OIDC. Introduce a provider-start/redirect path, state and nonce handling, callback processing, authorization-code exchange, and token/claim validation. Keep invite acceptance as the gating model by binding provider auth to an invite-backed expected identity before a session is created.</action>
  <verify>Add auth validation coverage that proves provider-start routes exist, callback handlers no longer depend on raw posted `providerAccountId`, and token-validation/state-handling code paths exist for all three providers.</verify>
  <acceptance_criteria>
    - Provider auth starts with a real redirect from the invite acceptance surface.
    - Callback handlers perform real code-based processing instead of trusting posted provider identity fields.
    - The flow validates state/nonce and provider identity before consuming the invite.
    - Google, Entra ID, and generic OIDC all share the same internal user model while preserving provider-specific config differences.
  </acceptance_criteria>
  <done>The product has a real provider-auth backbone instead of scaffolded callback placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce account-linking rules and safe invite-gated identity resolution</name>
  <files>src/auth/, src/storage/runtime-store.ts, src/app/api/auth/</files>
  <read_first>.planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-CONTEXT.md, src/auth/invitations.ts, src/auth/models.ts, src/storage/runtime-store.ts</read_first>
  <action>Implement the locked identity rules for provider auth: hard-fail on invited-email mismatch, hard-fail when a provider account is already linked elsewhere, allow provider linking later only from authenticated account management, and route existing-user membership conflicts into explicit separate-resolution behavior instead of silent auto-attachment.</action>
  <verify>Validation asserts the presence of explicit mismatch/conflict handling, provider-account uniqueness enforcement, and audit-event recording for both successful and failed provider acceptance paths.</verify>
  <acceptance_criteria>
    - Invite email mismatch is rejected explicitly.
    - Provider account reuse across different users is rejected explicitly.
    - Invite consumption occurs only after the provider identity passes validation.
    - Failure paths are auditable and do not silently widen access.
  </acceptance_criteria>
  <done>The provider flow is aligned with the invite-only security model and the milestone's identity-linking decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add platform-admin provider status visibility and callback-proof validation guidance</name>
  <files>src/app/settings/, src/auth/providers.ts, README.md, docs/operators.md, scripts/, .planning/</files>
  <read_first>.planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-CONTEXT.md, .planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-RESEARCH.md, src/app/settings/page.tsx, README.md, docs/operators.md</read_first>
  <action>Add a platform-admin surface that shows provider enablement/config presence, resolved callback URLs, and manually tracked verification state without exposing or editing secrets. Extend operator documentation and validation so the public-HTTPS proof path for Google, Entra ID, and one generic OIDC provider is explicit and auditable.</action>
  <verify>Validation confirms the provider-status surface exists, remains platform-admin only, never exposes secrets, and documents the required public-host callback proof path for closing `AUTH-05`.</verify>
  <acceptance_criteria>
    - Platform admins can see provider configuration presence and callback URLs safely.
    - Verification status can be tracked manually in the product surface.
    - Docs explain the real public-HTTPS validation path for Google, Entra ID, and one generic OIDC provider.
    - The phase adds explicit validation coverage for the new provider surface and callback proof contract.
  </acceptance_criteria>
  <done>The milestone has an operator-visible, secret-safe auth configuration/status surface plus a clear proof path for the callback requirement.</done>
</task>

</tasks>

<verification>
- [ ] Invite acceptance remains the entry point for provider auth
- [ ] Google, Entra ID, and generic OIDC use real redirect/callback flows
- [ ] Callback processing no longer trusts posted `providerAccountId` as proof of identity
- [ ] State and nonce validation exist in the provider flow
- [ ] Invite consumption happens only after verified identity and invited-email match
- [ ] Provider-account uniqueness conflicts fail explicitly
- [ ] Provider secrets remain env-only and are not editable/viewable in the UI
- [ ] Platform-admin provider-status surface exists and is authorization-protected
- [ ] Public-HTTPS callback proof path is documented for Google, Entra ID, and one generic OIDC provider
- [ ] Validation covers provider routes, identity rules, and provider-status surface
</verification>

<success_criteria>
- The provider auth gap called out in the milestone audit is closed in code, not just documented.
- The invite-only security model remains intact across provider flows.
- Platform admins can inspect provider readiness without handling secrets in the UI.
- The phase leaves a clear, testable path to re-run the milestone audit and satisfy `AUTH-02` through `AUTH-05`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-real-oauth-and-oidc-authentication-delivery/13-01-SUMMARY.md`
</output>
