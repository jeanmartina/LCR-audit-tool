---
phase: 06-real-pdf-export-delivery
plan: 01
type: execute
wave: 1
depends_on: [05-01]
files_modified: [package.json, package-lock.json, src/exports/pdf.ts, src/app/reporting/export/executive.pdf/route.ts, src/app/reporting/[targetId]/export/operational.pdf/route.ts, scripts/validate-reporting.js]
autonomous: true
requirements: [REP-01]
user_setup: []
must_haves:
  truths:
    - "The `.pdf` export routes must return real PDF artifacts, not text payloads with a PDF filename."
    - "PDF generation must be based on server-side HTML rendered into PDF."
    - "Executive and operational PDFs remain separate export variants with the same filter scope as the source screen."
    - "Validation must prove MIME type, PDF byte signature, and minimum report content."
  artifacts:
    - package.json
    - src/exports/pdf.ts
    - src/app/reporting/export/executive.pdf/route.ts
    - src/app/reporting/[targetId]/export/operational.pdf/route.ts
    - scripts/validate-reporting.js
  key_links:
    - src/app/reporting/export/executive.pdf/route.ts -> src/exports/pdf.ts
    - src/app/reporting/[targetId]/export/operational.pdf/route.ts -> src/exports/pdf.ts
    - src/exports/pdf.ts -> src/reporting/query-state.ts
    - scripts/validate-reporting.js -> src/exports/pdf.ts
---
<objective>
Close the final milestone blocker by replacing the current fake `.pdf` downloads with real HTML-to-PDF export delivery for the executive and operational report flows.
Purpose: Phase 6 must make the reporting UI's PDF actions truthful so `REP-01` is genuinely satisfied and milestone `v1.0` can pass audit.
Output: Real PDF renderer integration, PDF-returning export routes, and validation that proves actual PDF delivery.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add HTML-to-PDF rendering path for executive and operational reports</name>
  <files>package.json, package-lock.json, src/exports/pdf.ts</files>
  <read_first>.planning/phases/06-real-pdf-export-delivery/06-CONTEXT.md, .planning/phases/06-real-pdf-export-delivery/06-RESEARCH.md, src/exports/pdf.ts</read_first>
  <action>Introduce the dependency and rendering path needed for server-side HTML -> PDF generation. Refactor `src/exports/pdf.ts` so executive and operational exports produce real PDF bytes/artifacts instead of plain text bodies, while preserving distinct report layouts and the current serialized filter scope. Keep the executive report summary-oriented and the operational report evidence-oriented exactly as decided in the Phase 6 context.</action>
  <verify>`node scripts/validate-reporting.js pdf-bytes` exits 0 and prints `Reporting PDFs are real`.</verify>
  <acceptance_criteria>
    - The PDF export module no longer models output as plain text only.
    - Executive and operational exports remain separate report variants.
    - Returned bytes include a valid PDF signature prefix.
  </acceptance_criteria>
  <done>The codebase can generate real PDF artifacts for both report variants.</done>
</task>

<task type="auto">
  <name>Task 2: Wire export routes to return application/pdf artifacts</name>
  <files>src/app/reporting/export/executive.pdf/route.ts, src/app/reporting/[targetId]/export/operational.pdf/route.ts, src/exports/pdf.ts</files>
  <read_first>.planning/phases/06-real-pdf-export-delivery/06-CONTEXT.md, .planning/v1.0-MILESTONE-AUDIT.md, src/app/reporting/export/executive.pdf/route.ts, src/app/reporting/[targetId]/export/operational.pdf/route.ts</read_first>
  <action>Update the executive and operational export routes so they return `application/pdf` responses backed by the real PDF bytes produced in Task 1. Preserve the current query/filter parsing contract so each export still reflects the exact scope of the source screen. Keep route handlers thin and route all report assembly through the shared export module rather than duplicating reporting logic in the route layer.</action>
  <verify>`node scripts/validate-reporting.js pdf-routes` exits 0 and prints `Reporting PDF routes wired`.</verify>
  <acceptance_criteria>
    - Both `.pdf` routes return `application/pdf`.
    - Both routes preserve filename and current filter scope.
    - Route handlers no longer return `text/plain` payloads.
  </acceptance_criteria>
  <done>The reporting UI's PDF links now deliver real PDF downloads.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen PDF validation to close the milestone audit gap</name>
  <files>scripts/validate-reporting.js, src/exports/pdf.ts, src/app/reporting/export/executive.pdf/route.ts, src/app/reporting/[targetId]/export/operational.pdf/route.ts</files>
  <read_first>.planning/phases/06-real-pdf-export-delivery/06-CONTEXT.md, .planning/v1.0-MILESTONE-AUDIT.md, scripts/validate-reporting.js</read_first>
  <action>Extend `scripts/validate-reporting.js` with PDF-specific checks that prove the milestone blocker is closed: MIME type assertions for the route handlers, byte-signature assertions for generated PDF output, and minimum content assertions that show executive and operational reports contain the expected report structure. Keep the checks targeted and local to the PDF export path so they can run without requiring a brand-new full-build pipeline.</action>
  <verify>`node scripts/validate-reporting.js pdf-audit` exits 0 and prints `Reporting PDF audit gap closed`.</verify>
  <acceptance_criteria>
    - Validation checks fail if routes regress to `text/plain`.
    - Validation checks fail if generated output is not a real PDF.
    - Validation checks prove minimum expected report content for both variants.
  </acceptance_criteria>
  <done>The milestone audit has concrete evidence that PDF export is real and not just mislabeled text.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-reporting.js pdf-bytes` exits 0
- [ ] `node scripts/validate-reporting.js pdf-routes` exits 0
- [ ] `node scripts/validate-reporting.js pdf-audit` exits 0
- [ ] Executive and operational routes return `application/pdf`
- [ ] Generated PDF bytes begin with `%PDF-`
- [ ] Validation proves minimum expected executive and operational report content
</verification>

<success_criteria>
- The reporting UI's PDF actions now produce genuine PDF artifacts
- Executive and operational report exports preserve current filter scope
- The exact milestone blocker identified in `v1.0-MILESTONE-AUDIT.md` is closed
</success_criteria>

<output>
After completion, create `.planning/phases/06-real-pdf-export-delivery/06-01-SUMMARY.md`
</output>
