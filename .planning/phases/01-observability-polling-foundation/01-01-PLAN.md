---
phase: 01-observability-polling-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/inventory/targets.ts, src/polling/scheduler.ts, src/storage/coverage-records.ts, infra/healthchecks-config.yml]
autonomous: true
requirements: [MON-01, MON-02, CFG-01]
user_setup: []
must_haves:
  truths:
    - "Each poll writes a Timescale row capturing status, retry duration, outage flag, and coverage window metadata so the SLA metrics service has the raw evidence it needs."
    - "Inventory metadata (interval, timeout, alert recipients) drives scheduler execution without code changes."
  artifacts:
    - src/polling/scheduler.ts
    - src/inventory/targets.ts
  key_links:
    - .planning/phases/01-observability-polling-foundation/01-CONTEXT.md
---
<objective>
Build the telemetry pipeline that keeps every configured LCR or supplemental certificate under scheduled polling, records availability/outage details, and persists coverage events for downstream SLA metrics and alerting.
Purpose: Phase 1 must deliver the raw data that Verification and Alerting layers rely on; without these records Phases 2+ cannot run.
Output: Inventory service, scheduler worker, Timescale coverage records, and Healthchecks configuration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Define inventory metadata and targets</name>
  <files>src/inventory/targets.ts, infra/healthchecks-config.yml</files>
  <read_first>.planning/phases/01-observability-polling-foundation/01-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md</read_first>
  <action>Create `src/inventory/targets.ts` containing a `Target` interface (id/slug, url, type (LCR/certificate), interval_seconds, timeout_seconds, alert_email, coverage_gap_threshold, enabled) plus helpers to load/edit per-target metadata. Render `infra/healthchecks-config.yml` with a reusable heartbeat endpoint template so each poll can call Healthchecks with `target_id` tags.</action>
  <verify>`node scripts/validate-inventory.js` prints "Inventory schema valid" (script is hip to the new interface).</verify>
  <acceptance_criteria>
    - `src/inventory/targets.ts` declares interface `Target { id: string; slug: string; url: string; intervalSeconds: number; timeoutSeconds: number; alertEmail?: string; enabled: boolean; }`
    - `infra/healthchecks-config.yml` documents `heartbeat_url` template and a placeholder `{{target_id}}` tag.
    - Comments mention that `intervalSeconds` defaults to 600 when unspecified.
  </acceptance_criteria>
  <done>Inventory metadata is code-first and referenced by scheduler/healthchecks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement per-target scheduler worker</name>
  <files>src/polling/scheduler.ts</files>
  <read_first>src/inventory/targets.ts, .planning/phases/01-observability-polling-foundation/01-CONTEXT.md</read_first>
  <action>Implement `runScheduledPolls()` inside `src/polling/scheduler.ts` that loads all enabled targets, calculates `nextCheckAt = lastCheckAt + intervalSeconds`, and for each due target performs an HTTP GET/HEAD (timeout = target.timeoutSeconds) against the configured URL. Record the response status, duration, and `coverageLost` flag when HTTP ≠ 200 or timeout. After each successful fetch emit a Healthchecks heartbeat that tags `target_id`. Serialize results into Timescale using the schema defined in Task 3.</action>
  <verify>`node scripts/validate-scheduler.js` outputs "Scheduler polls ready".</verify>
  <acceptance_criteria>
    - `src/polling/scheduler.ts` exports `runScheduledPolls()` that iterates `targets.filter(t => due)` and calls `recordPollResult(target, status, duration, coverageLost)`.
    - Each HTTP client call uses `timeoutSeconds` from the target and treats non-200 responses as coverage loss.
    - A comment documents that Healthchecks POST uses `infra/healthchecks-config.yml`'s endpoint template to include `target_id`.
  </acceptance_criteria>
  <done>Scheduler worker can dispatch polls and mark coverage loss on failures.</done>
</task>

<task type="auto">
  <name>Task 3: Persist coverage records and gap windows</name>
  <files>src/storage/coverage-records.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-observability-polling-foundation/01-RESEARCH.md</read_first>
  <action>Create `src/storage/coverage-records.ts` with `recordPollResult(targetId, status, duration, hash, coverageLost)` that inserts into Timescale hypertables: `polls` (target_id, status, duration_ms, occurred_at, coverage_lost boolean) and `coverage_gaps` (start_ts, end_ts, target_ids text[]). When a target is down and no newer LCR is available, insert or extend the open `coverage_gaps` row keyed by `target_id` and `start_ts`. Export `getCoverageStatus()` to help the metrics layer aggregate per-window SLA percentages.</action>
  <verify>`node scripts/validate-storage.js` prints "Coverage schema ready".</verify>
  <acceptance_criteria>
    - Table comments show `polls` stores `status`, `durationMs`, `coverageLost`, and `coverage_gaps` stores `targetIds`.
    - There is logic to `INSERT ... ON CONFLICT` to extend running gaps when successive polls fail.
    - Exported `getCoverageStatus()` returns the latest `coverage_gaps` window and the last success timestamp for each target.
  </acceptance_criteria>
  <done>Timescale tables capture poll results and coverage gap windows for SLA calculations.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-scheduler.js` and `node scripts/validate-storage.js` both exit 0
- [ ] `src/polling/scheduler.ts` references `src/storage/coverage-records.ts`
- [ ] Coverage gap metadata includes `target_id`, `start_ts`, and `end_ts`
</verification>

<success_criteria>
- All inventory metadata and scheduler code exist
- Coverage records persist polls and gap windows
- Healthchecks heartbeat metadata is documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-observability-polling-foundation/01-01-SUMMARY.md`
</output>
