---
phase: 03-reporting-compliance-governance
plan: 01
type: execute
wave: 1
depends_on: [02-01]
files_modified: [src/inventory/targets.ts, src/reporting/read-models.ts, src/reporting/timeline.ts, src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx, src/exports/csv.ts, src/exports/pdf.ts, scripts/validate-reporting.js]
autonomous: true
requirements: [REP-01, REP-02]
user_setup: []
must_haves:
  truths:
    - "The main dashboard must expose per-target status, latest unavailability, SLA by period, upcoming LCR expiration, open alerts, recent alerts, and compound filters."
    - "Detail views must show the full evidence chain: poll history, coverage gaps, alert history, validation failures, valid LCR metadata, prior snapshots, and a unified event timeline."
    - "Every CSV/PDF export must carry the applied filters and generation timestamp, and non-snapshot evidence retention defaults to 180 days with per-target override."
  artifacts:
    - src/reporting/read-models.ts
    - src/app/reporting/page.tsx
    - src/app/reporting/[targetId]/page.tsx
    - src/exports/csv.ts
    - src/exports/pdf.ts
  key_links:
    - src/app/reporting/page.tsx -> src/reporting/read-models.ts
    - src/app/reporting/[targetId]/page.tsx -> src/reporting/timeline.ts
    - src/exports/csv.ts -> src/reporting/read-models.ts
    - src/exports/pdf.ts -> src/reporting/read-models.ts
    - src/exports/pdf.ts -> src/reporting/timeline.ts
---
<objective>
Build the reporting and compliance surface that turns Phase 1/2 evidence into an auditable dashboard, detailed drill-downs, and exportable artifacts.
Purpose: Phase 3 is the first user-facing layer that proves SLA and coverage history to compliance and engineering without changing upstream monitoring semantics.
Output: Reporting read models, dashboard UI, detail audit views, CSV exports, and two PDF variants.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add reporting read models and retention defaults</name>
  <files>src/inventory/targets.ts, src/reporting/read-models.ts, src/reporting/timeline.ts</files>
  <read_first>.planning/phases/03-reporting-compliance-governance/03-CONTEXT.md, .planning/phases/03-reporting-compliance-governance/03-RESEARCH.md, src/storage/coverage-records.ts, src/metrics/sla-metrics.ts, src/alerting/alert-policy.ts, src/inventory/targets.ts</read_first>
  <action>Extend `src/inventory/targets.ts` with configurable evidence-retention settings per target and document a default of 180 days for polls, alerts, and coverage gaps, while keeping LCR snapshots permanent. Create `src/reporting/read-models.ts` to aggregate current status, latest unavailability, SLA over a requested period, next LCR expiration, open alerts, recent alerts, and filter metadata from existing storage/metrics modules. Create `src/reporting/timeline.ts` to normalize poll, alert, validation, expiration, recovery, and coverage-gap events into one audit timeline structure used by both UI and exports.</action>
  <verify>`node scripts/validate-reporting.js read-models` exits 0 and prints `Reporting read models ready`.</verify>
  <acceptance_criteria>
    - `src/inventory/targets.ts` exposes retention configuration with a 180-day default for non-snapshot evidence.
    - `src/reporting/read-models.ts` returns dashboard-ready rows containing current status, last unavailability, SLA period metrics, next expiration, and alert summaries.
    - `src/reporting/timeline.ts` returns a unified event list containing poll, alert, validation, expiration, recovery, and coverage-gap entries.
  </acceptance_criteria>
  <done>Reporting can answer dashboard and drill-down queries from existing stored evidence without changing upstream writes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard and audit drill-down UI</name>
  <files>src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx</files>
  <read_first>.planning/phases/03-reporting-compliance-governance/03-CONTEXT.md, src/reporting/read-models.ts, src/reporting/timeline.ts</read_first>
  <action>Create the main reporting page in `src/app/reporting/page.tsx` using the existing Next.js 16 + TypeScript stack, centered on a dense, filterable table for lists/certificates. Surface current status, latest unavailability, SLA by period, upcoming LCR expiration, open alerts, recent alerts, and compound filters for source, issuer, criticality, responsible party, and date range. Create `src/app/reporting/[targetId]/page.tsx` to expose poll history with timestamp, HTTP status, and timeout outcomes; coverage-gap windows with start, end, and duration; alert history with severity and recipients; latest valid LCR metadata; prior snapshots; validation failure reasons; and the unified timeline for a single target. The detail view must also support date-range and compound filtering over evidence records.</action>
  <verify>`node scripts/validate-reporting.js ui` exits 0 and prints `Reporting UI sections ready`.</verify>
  <acceptance_criteria>
    - The main reporting page contains all required summary/data areas and exposes date-range plus compound filters.
    - The target detail page exposes all six evidence areas required by the phase context, including poll history with timestamp/HTTP status/timeout outcomes, coverage gaps with start/end/duration, and alert history with severity/recipients.
    - The target detail page supports date-range and compound filtering across evidence records, not only the main dashboard table.
    - UI copy or comments make clear which timestamp/filter context the user is viewing.
  </acceptance_criteria>
  <done>The reporting UI provides both an at-a-glance operational surface and a complete per-target audit path.</done>
</task>

<task type="auto">
  <name>Task 3: Deliver CSV/PDF exports with traceability metadata</name>
  <files>src/exports/csv.ts, src/exports/pdf.ts, scripts/validate-reporting.js</files>
  <read_first>.planning/phases/03-reporting-compliance-governance/03-CONTEXT.md, src/reporting/read-models.ts, src/reporting/timeline.ts</read_first>
  <action>Create `src/exports/csv.ts` to export the filtered main table, per-target detail, coverage-gap history, alert history, and LCR/snapshot inventory. Create `src/exports/pdf.ts` to generate two outputs: an operational PDF with detailed evidence and an executive PDF with consolidated summary. Update or create `scripts/validate-reporting.js` so it verifies the presence of all reporting sections and checks that export helpers include applied filters and generation timestamp in their output payloads.</action>
  <verify>`node scripts/validate-reporting.js exports` exits 0 and prints `Reporting exports ready`.</verify>
  <acceptance_criteria>
    - `src/exports/csv.ts` supports all requested CSV export shapes.
    - `src/exports/pdf.ts` produces distinct operational and executive output builders.
    - Validation script assertions check for applied filters and generation timestamp in both CSV/PDF export structures.
  </acceptance_criteria>
  <done>Exports satisfy audit traceability requirements and distinguish operational vs executive reporting needs.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-reporting.js read-models` exits 0
- [ ] `node scripts/validate-reporting.js ui` exits 0
- [ ] `node scripts/validate-reporting.js exports` exits 0
- [ ] Dashboard rows include status, last unavailability, SLA period, next expiration, and alert summary fields
- [ ] Detail timeline includes poll, alert, validation, expiration, recovery, and coverage-gap events
</verification>

<success_criteria>
- Dashboard covers the required operational/compliance fields and filters
- Drill-down surfaces the full evidence chain for a single target
- CSV and both PDF variants are traceable and audit-ready
- Retention policy is configurable per target with a documented 180-day default
</success_criteria>

<output>
After completion, create `.planning/phases/03-reporting-compliance-governance/03-01-SUMMARY.md`
</output>
