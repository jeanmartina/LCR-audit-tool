---
phase: 10-group-scoped-reporting-and-predictive-monitoring
plan: 01
type: execute
wave: 1
depends_on: [09-01]
files_modified: [src/storage/runtime-store.ts, src/reporting/, src/exports/, src/app/, src/auth/, scripts/validate-reporting-phase10.js]
autonomous: true
requirements: [GRP-04, MON-03, MON-04, REP-03]
user_setup:
  - representative certificates with effective `trustSource`, `pki`, `jurisdiction`, and `nextUpdate` metadata make predictive/reporting verification more realistic
must_haves:
  truths:
    - "Certificate view remains the default reporting mode."
    - "CRL view is complementary and must not widen authorization."
    - "Predictive alerts start 3 days before expiration and use `nextUpdate` for publication-delay logic."
    - "Predictive preferences are user-specific and configurable by groups, severities, and predictive types."
    - "Settings must include at least light and dark themes in this phase."
  artifacts:
    - src/reporting/
    - src/exports/
    - src/app/reporting/
    - src/app/settings/
    - scripts/validate-reporting-phase10.js
  key_links:
    - .planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-CONTEXT.md
    - .planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-RESEARCH.md
    - .planning/REQUIREMENTS.md
---
<objective>
Extend the reporting product surface so v1.1 gains group-safe CRL viewing, predictive alerting, structured multi-PKI tagging, and a dedicated settings surface for predictive preferences and theme choice.
Purpose: Phase 9 created certificate-first domain truth; Phase 10 must now turn that truth into richer, authorization-safe reporting behavior before the i18n and packaging phases land.
Output: predictive/user-settings persistence, certificate+CRL reporting projections, reporting/export updates, and a dedicated settings page with theme and predictive controls.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add predictive state, user settings, and effective structured-tag support</name>
  <files>src/storage/runtime-store.ts, src/auth/, src/inventory/, src/reporting/, scripts/validate-reporting-phase10.js</files>
  <read_first>.planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-CONTEXT.md, .planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-RESEARCH.md, .planning/REQUIREMENTS.md, src/storage/runtime-store.ts, src/inventory/certificate-admin.ts</read_first>
  <action>Extend the durable model with user settings for theme and predictive alert preferences, predictive event/state storage driven by `nextUpdate`, and effective structured-tag support for `trustSource`, `pki`, and `jurisdiction`. The implementation should support user-level predictive preferences by groups, severities, and predictive types, and expose effective structured tag values for reporting to consume. Preserve group/certificate authorization boundaries as the data model expands.</action>
  <verify>`node scripts/validate-reporting-phase10.js model` exits 0 and prints `Phase 10 reporting model ready`.</verify>
  <acceptance_criteria>
    - Predictive event/state structures exist for upcoming expiration and publication delay.
    - User settings can represent theme plus predictive group/severity/type preferences.
    - Structured tags are represented as effective reporting-facing dimensions rather than ad hoc page constants.
    - Validation script is introduced in this task and asserts the required persistence/read-model artifacts.
  </acceptance_criteria>
  <done>The codebase has durable predictive and settings foundations instead of page-local assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend reporting, query state, timeline, and exports for certificate mode, CRL mode, predictive state, and structured tags</name>
  <files>src/reporting/, src/exports/, src/app/reporting/, scripts/validate-reporting-phase10.js</files>
  <read_first>.planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-CONTEXT.md, .planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-RESEARCH.md, src/reporting/read-models.ts, src/reporting/query-state.ts, src/exports/csv.ts, src/exports/pdf.ts</read_first>
  <action>Expand the existing reporting contract so it can express both certificate view and CRL view, structured-tag filters/groupings, and predictive-risk state. Update read-models, timeline, CSV exports, and PDF exports so they preserve authorization, active reporting mode, active filters, and predictive context. The CRL view should be more than a trivial flat list, but it should still remain a constrained projection built only from certificates/groups the principal can already access.</action>
  <verify>`node scripts/validate-reporting-phase10.js reporting` exits 0 and prints `Phase 10 reporting views ready`.</verify>
  <acceptance_criteria>
    - Dashboard can switch between certificate and CRL view without changing entitlements.
    - Filters/groupings support `trustSource`, `pki`, and `jurisdiction`.
    - Predictive indicators appear in reporting and predictive events are visible in timeline/export context.
    - Exports preserve active mode, filters, tags, and predictive context.
  </acceptance_criteria>
  <done>Reporting and exports can now express the richer product view without breaking authorization rules.</done>
</task>

<task type="auto">
  <name>Task 3: Deliver the dedicated settings page, theme switching, and final reporting surface updates</name>
  <files>src/app/, src/auth/, src/reporting/, scripts/validate-reporting-phase10.js</files>
  <read_first>.planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-CONTEXT.md, .planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-RESEARCH.md, src/app/layout.tsx, src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx</read_first>
  <action>Create a dedicated settings surface where authenticated users can configure theme, predictive alert enablement, group scope, severities, and predictive types. Wire at least light and dark themes into the app shell so the setting actually changes the product surface. Update the reporting pages to expose the new reporting mode switch, predictive indicators, and structured-tag filters while keeping the certificate view as the default and the drill-down certificate-scoped.</action>
  <verify>`node scripts/validate-reporting-phase10.js settings` exits 0 and prints `Phase 10 settings and theming ready`.</verify>
  <acceptance_criteria>
    - A dedicated settings page exists for predictive preferences and theme choice.
    - The app shell can render at least light and dark themes based on user preference.
    - Dashboard and drill-down surfaces expose the new reporting-mode and predictive/tag affordances.
    - Authorization remains enforced in backend/read-model/export paths, not only in the UI.
  </acceptance_criteria>
  <done>The product now exposes predictive/reporting behavior and theme settings as real user-facing surfaces.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-reporting-phase10.js model` exits 0
- [ ] `node scripts/validate-reporting-phase10.js reporting` exits 0
- [ ] `node scripts/validate-reporting-phase10.js settings` exits 0
- [ ] Certificate mode remains default and CRL mode is authorization-safe
- [ ] Predictive alert logic uses `nextUpdate` and the 3-day window
- [ ] User preferences cover group, severity, and predictive type filters
- [ ] Structured tag filtering/grouping exists for `trustSource`, `pki`, and `jurisdiction`
- [ ] Dedicated settings page exists and light/dark theme switching works
</verification>

<success_criteria>
- Reporting now supports certificate and CRL reading modes without weakening auth boundaries
- Predictive monitoring is visible and configurable per user
- Structured multi-PKI / trust-source filtering is available in reporting
- The app has a real settings surface and theme preference foundation before Phase 11 i18n work
</success_criteria>

<output>
After completion, create `.planning/phases/10-group-scoped-reporting-and-predictive-monitoring/10-01-SUMMARY.md`
</output>
