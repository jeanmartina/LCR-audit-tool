---
phase: 19
plan: 19-02
name: Trust-List Provenance Surfaces and Final Validation
wave: 2
depends_on:
  - 19-01
requirements:
  - TSL-04
  - TSL-05
  - TSL-07
autonomous: true
files_modified:
  - src/trust-lists/admin.ts
  - src/app/admin/trust-lists/page.tsx
  - src/app/admin/certificates/[certificateId]/page.tsx
  - src/i18n/index.ts
  - scripts/validate-trust-list-projection.js
  - scripts/validate-all.js
---

# Phase 19 Plan 19-02: Trust-List Provenance Surfaces and Final Validation

**Created:** 2026-04-22
**Status:** Ready for execution

## Objective

Expose trust-list projection provenance in admin and certificate-detail surfaces and complete validation so users can distinguish trust-list-derived assets from manual/ZIP imports without SQL access.

## Scope

### In Scope

- Extend admin trust-list summaries with projection counts and latest projection failure reason.
- Expose certificate-level trust-list provenance in certificate admin/detail or an equivalent admin read model.
- Preserve existing reporting behavior without making enriched reporting filters/labels a Phase 19 requirement.
- Add localized copy for provenance and skipped unchanged behavior.
- Run full build/typecheck/validate-all.

### Out of Scope

- Full guided operator recovery workflows; Phase 20 owns them.
- Executive summary dashboard and richer reporting analytics; Phase 21 owns them.
- Non-admin trust-list management.

## Canonical References

- `.planning/phases/19-trust-list-certificate-projection-and-reimport/19-CONTEXT.md`
- `.planning/phases/19-trust-list-certificate-projection-and-reimport/19-UI-SPEC.md`
- `.planning/phases/19-trust-list-certificate-projection-and-reimport/19-VALIDATION.md`
- `src/trust-lists/admin.ts`
- `src/app/admin/trust-lists/page.tsx`
- `src/app/admin/certificates/[certificateId]/page.tsx`
- `src/i18n/index.ts`

## Threat Model

- UI must not imply legal trust-policy validation; keep wording to integrity/provenance.
- Any supporting reporting/read-model touch must not leak sources across unauthorized groups.
- Provenance display must degrade gracefully for manual/ZIP certificates.
- Skipped unchanged projections should be presented as successful no-op behavior, not hidden failures.

## Implementation Tasks

<task id="19-02-T1" title="Add provenance read helpers for admin surfaces">
  <read_first>
    - `src/trust-lists/admin.ts`
    - `src/storage/runtime-store.ts`
    - `src/inventory/certificate-admin.ts`
  </read_first>
  <action>
    Extend trust-list admin/read helpers:
    - source summaries include latest projection counts: imported, updated, skipped unchanged, duplicate skipped, failed;
    - source summaries include latest projection failure reason when available;
    - add helper to fetch latest trust-list provenance for a certificate ID, returning source label/URL, snapshot digest/sequence/territory, run ID, projection status/change reason, and source path.
    Do not require enriched reporting filters/labels in this phase.
  </action>
  <acceptance_criteria>
    - `src/trust-lists/admin.ts` reads `listTrustListCertificateProjections`.
    - `src/trust-lists/admin.ts` exposes skipped/updated/failed projection counts or summary fields.
    - `src/trust-lists/admin.ts` exports a certificate provenance helper or equivalent read model.
  </acceptance_criteria>
</task>

<task id="19-02-T2" title="Expose trust-list projection summary on admin trust-list page">
  <read_first>
    - `src/app/admin/trust-lists/page.tsx`
    - `src/trust-lists/admin.ts`
    - `.planning/phases/19-trust-list-certificate-projection-and-reimport/19-UI-SPEC.md`
  </read_first>
  <action>
    Update `/admin/trust-lists` to show projection-level counts and explanations:
    - imported;
    - updated;
    - skipped unchanged;
    - skipped duplicate;
    - failed;
    - latest projection failure reason when present.

    Keep the existing source/snapshot metadata and manual sync action.
  </action>
  <acceptance_criteria>
    - `src/app/admin/trust-lists/page.tsx` contains UI labels for skipped unchanged projections.
    - `src/app/admin/trust-lists/page.tsx` renders projection counts from admin summaries.
    - `src/app/admin/trust-lists/page.tsx` still uses `StatusPill` and Phase 16 primitives.
  </acceptance_criteria>
</task>

<task id="19-02-T3" title="Expose certificate-level trust-list provenance">
  <read_first>
    - `src/app/admin/certificates/[certificateId]/page.tsx`
    - `src/inventory/certificate-admin.ts`
    - `src/trust-lists/admin.ts`
    - `src/components/ui/primitives.tsx`
  </read_first>
  <action>
    Add a provenance panel or equivalent section for certificates whose `sourceType` is `trust-list`:
    - source label and URL;
    - snapshot sequence, territory, and digest;
    - sync run ID;
    - projection status/change reason;
    - extracted source path/ordinal when available.

    Manual/ZIP certificates should either omit the panel or show a simple non-trust-list source label without errors.
  </action>
  <acceptance_criteria>
    - `src/app/admin/certificates/[certificateId]/page.tsx` displays trust-list provenance for trust-list certificates.
    - The page handles missing provenance gracefully.
    - User-facing copy is localized through `src/i18n/index.ts`.
  </acceptance_criteria>
</task>

<task id="19-02-T4" title="Preserve reporting compatibility without enriched labels">
  <read_first>
    - `src/reporting/read-models.ts`
    - `src/app/reporting/page.tsx`
    - `src/i18n/index.ts`
  </read_first>
  <action>
    Review reporting read models for compatibility with trust-list projection records. Only make minimal changes if required to avoid regressions from the projection model. Do not add enriched reporting filters or provenance labels as a Phase 19 deliverable.
  </action>
  <acceptance_criteria>
    - Existing reporting pages continue to work for manual, ZIP, and trust-list-derived certificates.
    - No new enriched reporting filter or provenance label is required in Phase 19.
    - No cross-group source metadata leaks are introduced.
  </acceptance_criteria>
</task>

<task id="19-02-T5" title="Add localized provenance copy and final validation">
  <read_first>
    - `src/i18n/index.ts`
    - `scripts/validate-trust-list-projection.js`
    - `scripts/validate-all.js`
  </read_first>
  <action>
    Add `en`, `pt-BR`, and `es` strings for:
    - trust-list provenance;
    - skipped unchanged;
    - projection imported/updated/failed;
    - source label/URL;
    - snapshot sequence/digest/run ID.

    Extend `scripts/validate-trust-list-projection.js` to cover admin/certificate provenance and i18n expectations.
  </action>
  <acceptance_criteria>
    - `src/i18n/index.ts` contains trust-list provenance keys in all supported locales.
    - `node scripts/validate-i18n.js foundation` passes.
    - `node scripts/validate-i18n.js ui` passes.
    - `node scripts/validate-trust-list-projection.js` passes.
  </acceptance_criteria>
</task>

<task id="19-02-T6" title="Run final phase validation">
  <read_first>
    - `scripts/validate-all.js`
    - `package.json`
    - `.planning/phases/19-trust-list-certificate-projection-and-reimport/19-VALIDATION.md`
  </read_first>
  <action>
    Run final checks and fix any regressions:
    - `npm run build`
    - `npm run typecheck`
    - `node scripts/validate-all.js`

    Confirm Phase 19 summary records remaining deferred work for Phase 20 operator recovery and Phase 21 executive analytics.
  </action>
  <acceptance_criteria>
    - `npm run build` passes.
    - `npm run typecheck` passes.
    - `node scripts/validate-all.js` passes.
    - Phase 19 summary distinguishes completed provenance/change detection from Phase 20 recovery UX.
  </acceptance_criteria>
</task>

## Verification Commands

```bash
node scripts/validate-trust-list-projection.js
node scripts/validate-i18n.js foundation
node scripts/validate-i18n.js ui
npm run build
npm run typecheck
node scripts/validate-all.js
```

## Done Criteria

- Trust-list provenance is visible in admin source summaries and certificate detail surfaces.
- Source summaries explain skipped unchanged vs failed projections.
- Admin/certificate detail can distinguish trust-list-derived assets from manual/ZIP assets; reporting does not regress.
- Full validation passes.
