---
phase: 11-internationalization-and-english-first-product-surface
plan: 01
type: execute
wave: 1
depends_on: [10-01]
files_modified: [src/i18n/, src/app/, src/settings/, src/exports/, scripts/validate-i18n.js, .planning/]
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04]
user_setup:
  - use representative authenticated and unauthenticated flows when verifying locale behavior so login/invite and logged-in settings paths are both exercised
must_haves:
  truths:
    - "Translation keys are organized by domain/feature, such as `reporting.dashboard.title`."
    - "`en` is the canonical source locale; `pt-BR` and `es` mirror it."
    - "Missing translations in `pt-BR` or `es` fall back automatically to `en`."
    - "Users can choose locale before authentication and persist locale after login."
    - "CSV and PDF exports are also localized in this phase."
    - "Code, comments, and default technical documentation remain English-first."
  artifacts:
    - src/i18n/
    - src/app/auth/
    - src/app/reporting/
    - src/app/settings/
    - src/app/admin/certificates/
    - src/exports/
    - scripts/validate-i18n.js
  key_links:
    - .planning/phases/11-internationalization-and-english-first-product-surface/11-CONTEXT.md
    - .planning/phases/11-internationalization-and-english-first-product-surface/11-RESEARCH.md
    - .planning/REQUIREMENTS.md
---
<objective>
Internationalize the active product surface and normalize the current code/documentation flow around English-first engineering.
Purpose: Phase 10 delivered a stable multi-user reporting/settings surface; Phase 11 must make that surface translatable and consistent before deployment packaging lands.
Output: locale foundation, localized auth/reporting/settings/admin/export surfaces, locale selectors, validation coverage, and English-first normalization of touched active docs/artifacts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build the locale foundation, dictionary structure, and preference plumbing</name>
  <files>src/i18n/, src/storage/runtime-store.ts, src/settings/, src/app/layout.tsx, src/app/auth/, scripts/validate-i18n.js</files>
  <read_first>.planning/phases/11-internationalization-and-english-first-product-surface/11-CONTEXT.md, .planning/phases/11-internationalization-and-english-first-product-surface/11-RESEARCH.md, .planning/REQUIREMENTS.md, src/storage/runtime-store.ts, src/settings/preferences.ts, src/app/layout.tsx</read_first>
  <action>Add the i18n runtime foundation with domain-based dictionaries for `en`, `pt-BR`, and `es`, centralized locale resolution with `en` fallback, and locale persistence using the existing `preferredLocale` user field. Extend the settings/auth plumbing so locale can be selected before authentication and persisted after authentication. Keep the code and helper APIs English-first.</action>
  <verify>`node scripts/validate-i18n.js foundation` exits 0 and prints `I18n foundation ready`.</verify>
  <acceptance_criteria>
    - Locale dictionaries exist for `en`, `pt-BR`, and `es`.
    - Locale keys are organized by domain/feature rather than as flat global labels.
    - Locale resolution supports unauthenticated suggestion + authenticated persisted preference.
    - Login/invite surfaces expose locale selection.
  </acceptance_criteria>
  <done>The application has a real i18n foundation instead of ad hoc hardcoded page strings.</done>
</task>

<task type="auto">
  <name>Task 2: Translate auth, reporting, settings, and certificate-admin surfaces</name>
  <files>src/app/auth/, src/app/reporting/, src/app/settings/, src/app/admin/certificates/, src/i18n/, scripts/validate-i18n.js</files>
  <read_first>.planning/phases/11-internationalization-and-english-first-product-surface/11-CONTEXT.md, .planning/phases/11-internationalization-and-english-first-product-surface/11-RESEARCH.md, src/app/auth/page.tsx, src/app/reporting/page.tsx, src/app/settings/page.tsx, src/app/admin/certificates/page.tsx</read_first>
  <action>Refactor the existing in-scope product surfaces to remove hardcoded user-facing strings and replace them with localized message lookups. Cover auth, reporting, settings, certificate admin, and the main empty/error-facing UI states in those areas. Keep identifiers, comments, and engineering-facing text in English while making user-facing copy translatable.</action>
  <verify>`node scripts/validate-i18n.js ui` exits 0 and prints `I18n UI surfaces ready`.</verify>
  <acceptance_criteria>
    - Auth, reporting, settings, and certificate-admin surfaces resolve messages through the i18n layer.
    - In-scope hardcoded Portuguese/Spanish leftovers are removed from those surfaces.
    - Authenticated users can change locale from settings and see the selected locale reflected in the UI.
    - English remains the canonical engineering-facing language of the touched code.
  </acceptance_criteria>
  <done>The main product surfaces are now translatable and aligned with the English-first rule.</done>
</task>

<task type="auto">
  <name>Task 3: Localize CSV/PDF exports and normalize active docs/planning artifacts touched by this phase</name>
  <files>src/exports/, scripts/validate-i18n.js, .planning/PROJECT.md, .planning/STATE.md, .planning/phases/11-internationalization-and-english-first-product-surface/</files>
  <read_first>.planning/phases/11-internationalization-and-english-first-product-surface/11-CONTEXT.md, .planning/phases/11-internationalization-and-english-first-product-surface/11-RESEARCH.md, src/exports/csv.ts, src/exports/pdf.ts, src/exports/pdf-templates.js</read_first>
  <action>Localize CSV and PDF exports so user-facing labels/headings follow the active locale, then update validation to assert export localization and the three-language coverage. Normalize active planning/documentation artifacts touched in this phase to English-first wording where appropriate, without rewriting archived milestone history.</action>
  <verify>`node scripts/validate-i18n.js exports` exits 0 and prints `I18n exports and docs ready`.</verify>
  <acceptance_criteria>
    - CSV export headers and user-facing labels are localized.
    - PDF report titles, section headings, filter labels, and summary labels are localized.
    - Validation covers locale presence and fallback behavior.
    - Active touched planning/docs surfaces are normalized to English-first wording.
  </acceptance_criteria>
  <done>Exports and active touched docs now match the locale-aware, English-first product standard.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-i18n.js foundation` exits 0
- [ ] `node scripts/validate-i18n.js ui` exits 0
- [ ] `node scripts/validate-i18n.js exports` exits 0
- [ ] Locale dictionaries exist for `en`, `pt-BR`, and `es`
- [ ] Missing translations fall back to `en`
- [ ] Login/invite and authenticated settings both expose locale selection
- [ ] Auth, reporting, settings, certificate admin, CSV, and PDF surfaces are localized
- [ ] Touched code/comments/docs remain English-first
</verification>

<success_criteria>
- The product surface is translatable across the in-scope user flows and exports
- Per-user locale preference works end to end
- The codebase moves forward under an enforceable English-first standard
- Phase 12 can build deployment and operator docs on a stable multilingual product surface
</success_criteria>

<output>
After completion, create `.planning/phases/11-internationalization-and-english-first-product-surface/11-01-SUMMARY.md`
</output>
