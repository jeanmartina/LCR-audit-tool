---
phase: 15-public-https-oauth-oidc-proof-and-callback-validation
plan: 01
type: execute
wave: 1
depends_on: [13-01, 14-01]
files_modified: [.planning/, compose.yaml, Caddyfile, .env.example, README.md, docs/operators.md, scripts/]
autonomous: true
requirements: [AUTH-02, AUTH-05]
must_haves:
  truths:
    - "This phase proves only the Google provider flow on a real public HTTPS host."
    - "Microsoft Entra ID and generic OIDC are deferred and must not be treated as silently closed by this phase."
    - "The proof must use the packaged Docker/Caddy stack, not a source-only local path."
    - "The operator will execute the proof manually and report outcomes back step by step."
    - "Acceptance evidence must include invite start, callback completion, session creation, authorized reporting access, and logs/audit evidence."
  artifacts:
    - .planning/
    - compose.yaml
    - Caddyfile
    - .env.example
    - README.md
    - docs/operators.md
    - scripts/
  key_links:
    - .planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/15-CONTEXT.md
    - .planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/15-RESEARCH.md
    - .planning/v1.1-MILESTONE-AUDIT.md
    - .planning/REQUIREMENTS.md
---
<objective>
Close the remaining Google public-host auth-proof gap by preparing the packaged Docker/Caddy stack for operator-run public HTTPS validation, producing a step-by-step proof runbook and evidence kit, and aligning the milestone planning artifacts with the Google-only scope now chosen for v1.1.
Purpose: The code is ready, but the milestone still lacks recorded external proof. This phase turns that blocker into an operator-executable, auditable proof path instead of leaving the project at code readiness only.
Output: Google-only proof scope reflected in planning artifacts, packaged deployment guidance ready for a public host, and a reproducible manual proof kit with evidence capture for the next audit.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Align milestone scope and proof targets with the Google-only closure decision</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/v1.1-MILESTONE-AUDIT.md, .planning/STATE.md</files>
  <read_first>.planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/15-CONTEXT.md, .planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/15-RESEARCH.md, .planning/REQUIREMENTS.md, .planning/v1.1-MILESTONE-AUDIT.md</read_first>
  <action>Update the active planning artifacts so they explicitly reflect the decision that Phase 15 proves only Google in `v1.1`, while Entra ID and generic OIDC move to a future milestone. Make the remaining audit target for this phase precise enough that downstream verification does not pretend broader auth proof happened.</action>
  <verify>Validation of planning artifacts confirms Phase 15 maps to the Google-only auth proof scope and no active v1.1 artifact still implies that Entra ID and generic OIDC will be proven in this phase.</verify>
  <acceptance_criteria>
    - Active planning artifacts clearly state that Google is the only provider in scope for Phase 15.
    - Entra ID and generic OIDC are explicitly deferred rather than silently abandoned.
    - The milestone audit can distinguish the Google closure work from deferred provider work.
  </acceptance_criteria>
  <done>The remaining milestone scope is coherent and auditable before live proof begins.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the packaged deployment path for operator-run public-host proof</name>
  <files>compose.yaml, Caddyfile, .env.example, README.md, docs/operators.md, scripts/</files>
  <read_first>.planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/15-CONTEXT.md, compose.yaml, Caddyfile, .env.example, README.md, docs/operators.md</read_first>
  <action>Make the packaged Docker/Caddy stack ready for manual public-host validation with minimal operator ambiguity. Ensure the environment contract, host/origin expectations, and Google-provider prerequisites are documented precisely enough that the operator can bring the stack up on a public hostname without ad hoc interpretation.</action>
  <verify>Add or extend packaging validation so the project can assert the presence of the public-host proof contract, the Google-focused env/config guidance, and the Docker-first proof path.</verify>
  <acceptance_criteria>
    - The packaged stack remains the canonical path for proof.
    - Public-origin, Caddy, and Google callback expectations are documented in one coherent path.
    - The operator can configure the environment from the shipped artifacts without inventing missing settings.
  </acceptance_criteria>
  <done>The deployed Docker stack is ready to be used as the proof environment.</done>
</task>

<task type="auto">
  <name>Task 3: Produce a guided proof kit and evidence capture workflow for the manual Google run</name>
  <files>README.md, docs/operators.md, scripts/, .planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/</files>
  <read_first>.planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/15-CONTEXT.md, .planning/v1.1-MILESTONE-AUDIT.md, README.md, docs/operators.md</read_first>
  <action>Create a step-by-step operator runbook for the public-host Google proof, an evidence checklist, and a structured report template where the operator can record deployment facts, invite execution, callback success, session creation, reporting access, and logs/audit evidence. Keep the flow sequential so the operator can execute and report each checkpoint back.</action>
  <verify>Validation confirms the proof kit exists, is Google-specific, is Docker/Caddy-first, and requires all acceptance evidence needed for the next milestone audit.</verify>
  <acceptance_criteria>
    - The proof instructions are step-by-step rather than high-level.
    - Evidence collection covers invite start, HTTPS callback, session creation, reporting access, and logs/audit records.
    - The resulting report artifact is sufficient input for a follow-up audit.
  </acceptance_criteria>
  <done>The operator has a complete guided proof workflow instead of an implicit or memory-based process.</done>
</task>

</tasks>

<verification>
- [ ] Active planning artifacts reflect the Google-only Phase 15 scope
- [ ] Entra ID and generic OIDC are explicitly deferred from this phase
- [ ] The packaged Docker/Caddy stack remains the canonical proof path
- [ ] Public-host Google provider setup is documented clearly
- [ ] The proof runbook is step-by-step and operator-friendly
- [ ] The evidence checklist covers invite start, callback, session, reporting access, and logs/audit trail
- [ ] Validation covers the Google-focused proof kit and Docker-first packaging contract
</verification>

<success_criteria>
- Phase 15 converts the remaining auth blocker from an implicit “go test it somehow” gap into a precise, operator-executable proof workflow.
- The project no longer conflates Google proof with deferred Entra/OIDC proof.
- The packaged stack is the exact environment used for the next live auth validation.
- The follow-up milestone audit can consume a concrete proof report rather than only code and documentation claims.
</success_criteria>

<output>
After completion, create `.planning/phases/15-public-https-oauth-oidc-proof-and-callback-validation/15-01-SUMMARY.md`
</output>
