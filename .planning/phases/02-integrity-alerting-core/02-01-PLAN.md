---
phase: 02-integrity-alerting-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/validation/lcr-validator.ts, src/storage/coverage-records.ts, src/alerting/alert-policy.ts, src/metrics/sla-metrics.ts]
autonomous: true
requirements: [INT-01, ALT-01, ALT-02]
user_setup: []
must_haves:
  truths:
    - "Invalid LCRs are rejected and trigger alerts; verified LCRs persist metadata and blobs."
    - "Alerts follow severity-based cooldowns and continue until coverage returns."
    - "SLA warnings trigger on error-budget usage or time-without-coverage."
  artifacts:
    - src/validation/lcr-validator.ts
    - src/alerting/alert-policy.ts
    - src/metrics/sla-metrics.ts
  key_links:
    - .planning/phases/02-integrity-alerting-core/02-CONTEXT.md
---
<objective>
Implement validation, alerting, and SLA warning logic that protects the integrity of LCR data and keeps notifying until coverage recovers.
Purpose: Phase 2 ensures stored data is trustworthy and SLA alerts reflect real coverage risk.
Output: Validation module, alert policy, and SLA metrics layer.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add LCR validation module</name>
  <files>src/validation/lcr-validator.ts, src/storage/coverage-records.ts</files>
  <read_first>.planning/phases/02-integrity-alerting-core/02-CONTEXT.md, .planning/REQUIREMENTS.md, src/storage/coverage-records.ts</read_first>
  <action>Create `src/validation/lcr-validator.ts` with `validateLcr(signature, hash, issuer)` returning `{ valid: boolean, reason?: string }`. On failure, the caller must mark coverage lost and emit an alert. Update `src/storage/coverage-records.ts` to include metadata fields (`hash`, `issuer`, `thisUpdate`, `nextUpdate`, `status`) for verified LCRs only.</action>
  <verify>`node scripts/validate-storage.js` prints "Coverage schema ready".</verify>
  <acceptance_criteria>
    - `src/validation/lcr-validator.ts` exports `validateLcr` returning `valid` and optional `reason`.
    - `src/storage/coverage-records.ts` includes metadata fields for `hash`, `issuer`, `thisUpdate`, `nextUpdate`, `status` in the schema section.
    - Comments mention invalid LCRs must be rejected and alerting triggered.
  </acceptance_criteria>
  <done>Validation module exists and storage schema includes required metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement alert policy with cooldowns</name>
  <files>src/alerting/alert-policy.ts</files>
  <read_first>.planning/phases/02-integrity-alerting-core/02-CONTEXT.md, src/inventory/targets.ts</read_first>
  <action>Create `src/alerting/alert-policy.ts` implementing `shouldSendAlert(targetId, severity, lastSentAt)` with cooldowns: warning 30m, critical 10m. Add `classifySeverity(coverageLostDurationMs, errorBudgetUsed)` which triggers warning if either threshold is met and critical when coverage is down or validation fails. Ensure alerting continues until coverage returns.</action>
  <verify>`node -e "require('./src/alerting/alert-policy')"` exits 0.</verify>
  <acceptance_criteria>
    - `shouldSendAlert` enforces 30m warning and 10m critical cooldowns.
    - `classifySeverity` returns warning on error-budget OR time-without-coverage thresholds.
    - Code comments state alerts continue until recovery.
  </acceptance_criteria>
  <done>Alert policy implemented with severity cooldowns and dual warning triggers.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SLA metrics layer</name>
  <files>src/metrics/sla-metrics.ts, src/storage/coverage-records.ts</files>
  <read_first>.planning/REQUIREMENTS.md, src/storage/coverage-records.ts</read_first>
  <action>Create `src/metrics/sla-metrics.ts` with `calculateErrorBudget(coverageGaps, totalWindowMs)` and `timeWithoutCoverage(coverageGaps)` helpers. Export `shouldWarn(coverageGaps, errorBudgetThreshold, timeThresholdMs)` that returns true when either threshold is exceeded. Integrate with `getCoverageStatus()` output in `src/storage/coverage-records.ts`.</action>
  <verify>`node -e "require('./src/metrics/sla-metrics')"` exits 0.</verify>
  <acceptance_criteria>
    - `calculateErrorBudget` computes used budget from coverage gaps.
    - `shouldWarn` returns true when error-budget OR time-without-coverage thresholds trigger.
    - `timeWithoutCoverage` returns total ms of gaps in the window.
  </acceptance_criteria>
  <done>SLA metrics support warning triggers based on both error budget and time gap.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-storage.js` exits 0
- [ ] `node -e "require('./src/alerting/alert-policy')"` exits 0
- [ ] `node -e "require('./src/metrics/sla-metrics')"` exits 0
</verification>

<success_criteria>
- LCR validation rejects invalid artifacts and captures metadata for verified ones
- Alerts follow severity cooldowns and persist until recovery
- SLA warnings trigger on error budget or time without coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-integrity-alerting-core/02-01-SUMMARY.md`
</output>
