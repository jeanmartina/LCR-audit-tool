---
phase: 12-containerized-deployment-https-and-operator-documentation
plan: 01
type: execute
wave: 1
depends_on: [11-01]
files_modified: [Dockerfile, compose.yaml, Caddyfile, .env.example, .dockerignore, README.md, docs/, scripts/, .planning/]
autonomous: true
requirements: [AUTH-05, OPS-01, OPS-02, OPS-03, DOC-01]
user_setup:
  - provide a real public host only when validating real OAuth/OIDC callbacks; local HTTPS mode is sufficient for stack verification
must_haves:
  truths:
    - "The stack ships as `web + worker + postgres + caddy`."
    - "`worker` remains a single process separate from `web`."
    - "The normal runtime path goes through Caddy over HTTPS."
    - "A local HTTPS test mode exists even without a public domain."
    - "The default compose path does not require the operator to manually craft `DATABASE_URL`."
    - "Runtime secrets stay as environment variables in this phase."
    - "README.md stays concise and points to a separate operator-oriented document."
  artifacts:
    - Dockerfile
    - compose.yaml
    - Caddyfile
    - .env.example
    - .dockerignore
    - README.md
    - docs/
    - scripts/
  key_links:
    - .planning/phases/12-containerized-deployment-https-and-operator-documentation/12-CONTEXT.md
    - .planning/phases/12-containerized-deployment-https-and-operator-documentation/12-RESEARCH.md
    - .planning/REQUIREMENTS.md
---
<objective>
Package the application for practical local/staging deployment with Docker Compose, Caddy-managed HTTPS, and English operator documentation.
Purpose: Phase 11 stabilized the product surface; Phase 12 must turn it into a runnable operator package with a clear environment model and documented startup path.
Output: container artifacts, compose topology, Caddy HTTPS configuration, env contract, README, operator guide, and packaging validation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Package the application runtime for web and worker execution</name>
  <files>Dockerfile, .dockerignore, package.json, scripts/, src/</files>
  <read_first>.planning/phases/12-containerized-deployment-https-and-operator-documentation/12-CONTEXT.md, .planning/phases/12-containerized-deployment-https-and-operator-documentation/12-RESEARCH.md, package.json, scripts/runtime-smoke.js</read_first>
  <action>Create the container runtime artifacts needed to run the app as separate `web` and `worker` services from the same codebase where practical. Ensure the image build is consistent with the current Next.js/runtime scripts and does not introduce a second divergent execution model.</action>
  <verify>Introduce a packaging validation step that confirms the Docker/runtime artifacts exist and that the compose plan can reference distinct `web` and `worker` commands cleanly.</verify>
  <acceptance_criteria>
    - A Docker build artifact exists for the app runtime.
    - `web` and `worker` can run as separate services from the packaged app.
    - The packaging surface stays aligned with the current build/runtime commands.
  </acceptance_criteria>
  <done>The application has a real container packaging baseline instead of an implicit local-only runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Deliver Compose topology, internal database wiring, and Caddy HTTPS</name>
  <files>compose.yaml, Caddyfile, .env.example, scripts/, src/auth/config.ts</files>
  <read_first>.planning/phases/12-containerized-deployment-https-and-operator-documentation/12-CONTEXT.md, .planning/phases/12-containerized-deployment-https-and-operator-documentation/12-RESEARCH.md, src/auth/config.ts</read_first>
  <action>Add the `docker compose` topology for `web`, `worker`, `postgres`, and `caddy`, including internal networking, default database wiring, Caddy HTTPS ingress, and a local-vs-domain configuration path. Keep the default compose experience zero-manual for `DATABASE_URL` while retaining Postgres authentication and environment-driven callback/public-origin settings.</action>
  <verify>Validation asserts the compose topology contains the four required services, the default database connection path is internalized, and the Caddy configuration enforces the HTTPS-first runtime path.</verify>
  <acceptance_criteria>
    - Compose runs four services: `web`, `worker`, `postgres`, `caddy`.
    - The default stack path uses Caddy over HTTPS.
    - A local HTTPS test path exists alongside real-domain ACME support.
    - Operators do not need to hand-author `DATABASE_URL` for the default compose path.
  </acceptance_criteria>
  <done>The deployment topology is runnable and aligned with the auth/public-origin contract.</done>
</task>

<task type="auto">
  <name>Task 3: Write README, operator guide, and packaging validation</name>
  <files>README.md, docs/, scripts/, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>.planning/phases/12-containerized-deployment-https-and-operator-documentation/12-CONTEXT.md, .planning/phases/12-containerized-deployment-https-and-operator-documentation/12-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>Write a concise English `README.md` for GitHub users plus a separate English operator/deployment guide. The README must cover setup basics, env/config expectations, startup/shutdown/logging flows, HTTPS modes, provider variables, and link to the operator guide. Add validation coverage so docs/artifacts presence and key packaging assumptions are checked explicitly.</action>
  <verify>Validation confirms README and operator guide exist, README references the operator guide, and required deployment/doc artifacts are present.</verify>
  <acceptance_criteria>
    - `README.md` exists in English and covers setup/usage basics.
    - A separate operator-oriented document exists in English.
    - README points readers to the operator guide.
    - Validation checks packaging/doc artifact presence and key wiring assumptions.
  </acceptance_criteria>
  <done>The milestone has a deployable local/staging package and the minimum operator documentation needed to use it.</done>
</task>

</tasks>

<verification>
- [ ] Packaging artifacts exist: `Dockerfile`, `.dockerignore`, `compose.yaml`, `Caddyfile`, `.env.example`
- [ ] Compose defines `web`, `worker`, `postgres`, and `caddy`
- [ ] Default compose runtime does not require a hand-authored `DATABASE_URL`
- [ ] HTTPS is the normal path through Caddy
- [ ] Local HTTPS mode and real-domain callback/public-origin expectations are documented
- [ ] `README.md` exists in English
- [ ] A separate operator guide exists in English
- [ ] README points to the operator guide
- [ ] Validation covers packaging and documentation artifacts
</verification>

<success_criteria>
- The app can be packaged and run in a coherent local/staging topology.
- HTTPS and public-origin behavior are explicit and consistent with auth requirements.
- Operators can start, stop, inspect, and configure the stack without reverse-engineering the codebase.
- The repo has a GitHub-ready README plus a deeper operator document.
</success_criteria>

<output>
After completion, create `.planning/phases/12-containerized-deployment-https-and-operator-documentation/12-01-SUMMARY.md`
</output>
