---
phase: 28-settings-and-administration
plan: 04
type: execute
wave: 4
depends_on:
  - 28-03
files_modified:
  - src/storage/runtime-store.ts
  - src/trust-lists/admin.ts
  - src/app/api/admin/trust-lists/route.ts
  - src/app/api/admin/trust-lists/[sourceId]/route.ts
  - src/app/admin/trust-lists/page.tsx
  - src/app/admin/trust-lists/trust-list-source-wizard.tsx
  - src/app/admin/trust-lists/trust-list-admin-panel.tsx
  - src/app/settings/sections/trust-lists-tab.tsx
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Trust-list sources can show and edit metadata plus LOTL parent/subordinate relationships."
    - "The trust-list admin UI is reusable from both the dedicated admin page and the `Trust Lists` settings tab."
    - "Incorrect trust-list sources can be updated, removed, and synchronized from the same operator surface."
  artifacts:
    - path: "src/storage/runtime-store.ts"
      provides: "Trust-list source hierarchy and metadata persistence"
    - path: "src/trust-lists/admin.ts"
      provides: "Trust-list source update/delete helpers and hierarchy validation"
    - path: "src/app/api/admin/trust-lists/route.ts"
      provides: "Trust-list source create route"
    - path: "src/app/api/admin/trust-lists/[sourceId]/route.ts"
      provides: "Trust-list source PATCH/DELETE route"
    - path: "src/app/admin/trust-lists/trust-list-admin-panel.tsx"
      provides: "Reusable trust-list admin panel"
    - path: "src/app/admin/trust-lists/page.tsx"
      provides: "Dedicated admin page wrapper"
    - path: "src/app/settings/sections/trust-lists-tab.tsx"
      provides: "Settings tab entry point for trust-list management"
  key_links:
    - from: "src/trust-lists/admin.ts"
      to: "src/storage/runtime-store.ts"
      via: "trust-list hierarchy and metadata persistence"
      pattern: 'parentSourceId|updateTrustListSource|removeTrustListSource'
    - from: "src/app/admin/trust-lists/trust-list-admin-panel.tsx"
      to: "src/app/api/admin/trust-lists/[sourceId]/route.ts"
      via: "same-origin trust-list edit/delete forms"
      pattern: '/api/admin/trust-lists/.+/(PATCH|DELETE)'
    - from: "src/app/settings/sections/trust-lists-tab.tsx"
      to: "src/app/admin/trust-lists/trust-list-admin-panel.tsx"
      via: "shared reusable panel"
      pattern: 'TrustListAdminPanel'
---

<objective>
Bring trust-list management into the tabbed settings experience without losing the dedicated admin page.

Purpose: trust-list operators need hierarchy-aware source editing, clear metadata, and safe removal/sync controls, and the same reusable admin surface should be visible from both settings and the existing admin route.
Output: persisted trust-list hierarchy metadata, update/delete routes, and a reusable admin panel that serves both surfaces.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/storage/runtime-store.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/trust-lists/admin.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/admin/trust-lists/page.tsx
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/admin/trust-lists/trust-list-source-wizard.tsx
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/settings/sections/trust-lists-tab.tsx
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/api/admin/trust-lists/route.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/api/admin/trust-lists/[sourceId]/sync/route.ts

<interfaces>
From `src/storage/runtime-store.ts`:
```ts
export interface TrustListSourceRecord {
  id: string;
  label: string;
  url: string;
  enabled: boolean;
  groupIds: string[];
  createdByUserId: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface TrustListSourceSummary {
  source: TrustListSourceRecord;
  lastSnapshot: TrustListSnapshotRecord | null;
  lastRun: TrustListSyncRunRecord | null;
  lastSuccess: TrustListSyncRunRecord | null;
  lastFailure: TrustListSyncRunRecord | null;
  projectionCounts: TrustListProjectionCounts;
  latestProjectionFailureReason: string | null;
  latestRecovery: TrustListRecoveryGuidance | null;
}
```

From `src/trust-lists/admin.ts`:
```ts
export async function listTrustListSourcesForAdmin(
  actor?: AuthenticatedPrincipal,
): Promise<TrustListSourceSummary[]>;

export async function createTrustListSource(
  actor: AuthenticatedPrincipal | undefined,
  input: { label: string; url: string; enabled: boolean; groupIds: string[] },
): Promise<TrustListSourceRecord>;

export async function syncTrustListSourceNow(
  actor: AuthenticatedPrincipal | undefined,
  sourceId: string,
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trust-list hierarchy and metadata persistence</name>
  <files>src/storage/runtime-store.ts, src/trust-lists/admin.ts</files>
  <read_first>
    - src/storage/runtime-store.ts
    - src/trust-lists/admin.ts
    - src/trust-lists/sync.ts
  </read_first>
  <needs>The dedicated trust-list admin read model from earlier phases.</needs>
  <creates>A trust-list source model that can represent LOTL parent/child relationships and editable metadata fields.</creates>
  <has_checkpoint>false</has_checkpoint>
  <action>Add `parentSourceId: string | null` to the trust-list source record, persist it in the `trust_list_sources` table, and expose helpers in `src/trust-lists/admin.ts` to update a source's label, URL, enabled state, groups, and parent source. Make child/parent links explicit so a subordinate source can be placed under a LOTL source without losing the existing sync history or provenance records.</action>
  <acceptance_criteria>
    - `src/storage/runtime-store.ts` contains `parentSourceId` on `TrustListSourceRecord`
    - `src/trust-lists/admin.ts` exports an update helper for trust-list sources
    - the trust-list persistence layer still returns sync history and snapshot metadata
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-trust-list-foundation.js && npm run typecheck</automated>
  </verify>
  <done>Trust-list sources can store hierarchy and metadata changes without losing the existing sync trail.</done>
</task>

<task type="auto">
  <name>Task 2: Extract a reusable trust-list admin panel</name>
  <files>src/app/admin/trust-lists/page.tsx, src/app/admin/trust-lists/trust-list-source-wizard.tsx, src/app/admin/trust-lists/trust-list-admin-panel.tsx, src/app/settings/sections/trust-lists-tab.tsx</files>
  <read_first>
    - src/app/admin/trust-lists/page.tsx
    - src/app/admin/trust-lists/trust-list-source-wizard.tsx
    - src/trust-lists/admin.ts
    - src/app/settings/sections/trust-lists-tab.tsx
  </read_first>
  <needs>The hierarchy-aware source model from task 1.</needs>
  <creates>One reusable trust-list admin panel that powers both the dedicated admin page and the settings tab entry point.</creates>
  <has_checkpoint>false</has_checkpoint>
  <action>Move the current trust-list management UI into `src/app/admin/trust-lists/trust-list-admin-panel.tsx` and have both `/admin/trust-lists` and the `Trust Lists` tab render that same panel. Expand the panel so each source row can show label, URL, enabled state, group IDs, parent source, last success/failure, and sync summary, and add inline edit controls for label, URL, enabled, group IDs, and parent source. Keep the existing wizard preview and sync-now actions available from the same reusable panel.</action>
  <acceptance_criteria>
    - `src/app/admin/trust-lists/trust-list-admin-panel.tsx` exists and renders the shared source table/forms
    - `src/app/admin/trust-lists/page.tsx` imports the reusable panel instead of duplicating the whole UI
    - `src/app/settings/sections/trust-lists-tab.tsx` renders or links to the same reusable panel
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-trust-list-operator-ux.js && npm run typecheck</automated>
  </verify>
  <done>The trust-list management UI is reusable from both settings and the dedicated admin route.</done>
</task>

<task type="auto">
  <name>Task 3: Add trust-list update and delete routes</name>
  <files>src/app/api/admin/trust-lists/[sourceId]/route.ts, src/app/api/admin/trust-lists/route.ts</files>
  <read_first>
    - src/app/api/admin/trust-lists/route.ts
    - src/app/api/admin/trust-lists/[sourceId]/sync/route.ts
    - src/trust-lists/admin.ts
    - src/auth/request-security.ts
    - src/auth/authorization.ts
  </read_first>
  <needs>The reusable panel and hierarchy-aware persistence from tasks 1 and 2.</needs>
  <creates>Same-origin PATCH/DELETE routes for editing and removing trust-list sources while preserving sync behavior and hierarchy validation.</creates>
  <has_checkpoint>false</has_checkpoint>
  <action>Create `src/app/api/admin/trust-lists/[sourceId]/route.ts` with `PATCH` and `DELETE`. `PATCH` must update the source label, URL, enabled state, group IDs, and parent source through the new helper; `DELETE` must refuse to remove a source that still has subordinate children unless the hierarchy is reassigned first. Keep the existing create and sync routes intact, and make the settings/admin panel submit to these same-origin endpoints.</action>
  <acceptance_criteria>
    - `src/app/api/admin/trust-lists/[sourceId]/route.ts` exports `PATCH` and `DELETE`
    - deleting a parent source fails when subordinate children still point at it
    - the trust-list admin panel posts to the new route for edits and deletions
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-trust-list-foundation.js && npm run typecheck</automated>
  </verify>
  <done>Operators can update or remove trust-list sources through the tabbed settings surface without breaking hierarchy integrity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> trust-list admin routes | Untrusted form data can rewrite source metadata or hierarchy. |
| trust-list hierarchy -> runtime store | Parent/child links must stay acyclic and must not orphan descendants. |
| trust-list panel -> sync actions | Sync-now actions must remain same-origin and operator-scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | T | `src/app/api/admin/trust-lists/[sourceId]/route.ts` | mitigate | Validate source IDs, URLs, and parent links server-side and keep the route behind trust-list operator checks. |
| T-28-11 | I | `src/app/admin/trust-lists/trust-list-admin-panel.tsx` | mitigate | Render labels, URLs, and hierarchy fields as plain text and keep the wizard preview data text-only. |
| T-28-12 | D | `src/storage/runtime-store.ts` | mitigate | Prevent deletion of a source that still has subordinate children so the hierarchy cannot become inconsistent. |
</threat_model>

<verification>
- `node scripts/validate-trust-list-foundation.js` passes with the hierarchy-aware trust-list model and routes.
- `node scripts/validate-trust-list-operator-ux.js` passes once the reusable panel exposes edit/delete/sync controls.
- `npm run typecheck` passes after the trust-list source record and route changes.
</verification>

<success_criteria>
- Trust-list sources can be edited and removed from the operator surface.
- Hierarchy information is visible and validated instead of being implied.
- The dedicated admin page and the settings tab share one reusable trust-list panel.
</success_criteria>

<output>
After completion, create `.planning/phases/28-settings-and-administration/28-04-SUMMARY.md`
</output>
