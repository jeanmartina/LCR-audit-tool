---
phase: 07-production-readiness-cleanup
plan: 01
type: execute
wave: 1
depends_on: [06-01]
files_modified: [package.json, tsconfig.json, next-env.d.ts, scripts/validate-all.js, scripts/runtime-smoke.js, src/storage/runtime-store.ts, src/exports/pdf-templates.js, scripts/validate-reporting.js, .planning/phases/01-observability-polling-foundation/01-VALIDATION.md, .planning/phases/02-integrity-alerting-core/02-VALIDATION.md, .planning/phases/03-reporting-compliance-governance/03-VALIDATION.md, .planning/phases/04-runtime-inventory-polling-persistence-wiring/04-VALIDATION.md, .planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-VALIDATION.md, .planning/phases/06-real-pdf-export-delivery/06-VALIDATION.md]
autonomous: true
requirements: []
user_setup:
  - DATABASE_URL pointing to a real Postgres instance for the runtime smoke test
must_haves:
  truths:
    - "Phase 7 is cleanup and production-readiness hardening only; it must not reopen milestone scope."
    - "A real Postgres-backed runtime smoke path must exist and be reproducible through DATABASE_URL."
    - "One quality gate command must cover build, typecheck, and the project's validation scripts."
    - "PDF changes stay within minimal readability improvements on the current renderer."
    - "Nyquist validation artifacts for phases 1-6 must be created from shipped evidence, not by redefining completed scope."
  artifacts:
    - package.json
    - tsconfig.json
    - next-env.d.ts
    - scripts/validate-all.js
    - scripts/runtime-smoke.js
    - src/storage/runtime-store.ts
    - src/exports/pdf-templates.js
    - scripts/validate-reporting.js
    - .planning/phases/01-observability-polling-foundation/01-VALIDATION.md
    - .planning/phases/02-integrity-alerting-core/02-VALIDATION.md
    - .planning/phases/03-reporting-compliance-governance/03-VALIDATION.md
    - .planning/phases/04-runtime-inventory-polling-persistence-wiring/04-VALIDATION.md
    - .planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-VALIDATION.md
    - .planning/phases/06-real-pdf-export-delivery/06-VALIDATION.md
  key_links:
    - package.json -> scripts/validate-all.js
    - package.json -> scripts/runtime-smoke.js
    - scripts/runtime-smoke.js -> src/storage/runtime-store.ts
    - scripts/validate-all.js -> scripts/validate-runtime-wiring.js
    - scripts/validate-all.js -> scripts/validate-reporting.js
    - src/exports/pdf-templates.js -> src/exports/pdf-engine.js
---
<objective>
Harden the shipped v1 system for production readiness by adding a reproducible Postgres-backed smoke path, a single quality gate, minimal PDF readability polish, and the missing retroactive validation artifacts for completed phases.
Purpose: Phase 7 converts the audit's remaining non-blocking debt into explicit, repeatable readiness checks without reopening feature scope.
Output: build/typecheck/validation gate, live-Postgres smoke proof, polished current PDF templates, and `*-VALIDATION.md` artifacts for Phases 1-6.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Establish the production-readiness gate foundation</name>
  <files>package.json, tsconfig.json, next-env.d.ts, scripts/validate-all.js</files>
  <read_first>.planning/phases/07-production-readiness-cleanup/07-CONTEXT.md, .planning/phases/07-production-readiness-cleanup/07-RESEARCH.md, package.json</read_first>
  <action>Add the minimum TypeScript/Next project configuration and package scripts needed to support a single quality gate. Create `scripts/validate-all.js` as the consolidated entrypoint that runs the existing validation scripts in a stable order and reports one pass/fail result. Update `package.json` so maintainers have a single command for build, typecheck, and validation, instead of manually chaining ad hoc scripts. Keep the configuration minimal and directly tied to the existing codebase rather than turning this into a general CI redesign.</action>
  <verify>`node scripts/validate-all.js` exits 0 and prints `All project validations passed`, and the package-level quality command can invoke build + typecheck + validations from one entrypoint.</verify>
  <acceptance_criteria>
    - The repo has the minimum config required for a real `build` and `typecheck` command.
    - Existing validation scripts are orchestrated through one stable entrypoint rather than manual operator knowledge.
    - A single documented package-script gate exists for local production-readiness checks.
  </acceptance_criteria>
  <done>The project has one reproducible quality gate instead of scattered manual checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add a reproducible real-Postgres runtime smoke test</name>
  <files>scripts/runtime-smoke.js, src/storage/runtime-store.ts, package.json</files>
  <read_first>.planning/phases/07-production-readiness-cleanup/07-CONTEXT.md, .planning/phases/04-runtime-inventory-polling-persistence-wiring/04-CONTEXT.md, .planning/phases/04-runtime-inventory-polling-persistence-wiring/04-VERIFICATION.md, src/storage/runtime-store.ts</read_first>
  <action>Create a runtime smoke script that requires `DATABASE_URL`, initializes the runtime schema against a live Postgres database, writes representative target/poll/validation/alert/snapshot/coverage-gap records through the real runtime-store path, and reads them back to prove persistence wiring actually works with a live connection. Expose that smoke path through package scripts so it is reproducible in local or staging environments. Keep the smoke focused on proving real runtime behavior rather than building a full integration test suite.</action>
  <verify>`DATABASE_URL=... node scripts/runtime-smoke.js` exits 0 and prints `Runtime smoke passed against Postgres`.</verify>
  <acceptance_criteria>
    - The smoke script fails clearly when `DATABASE_URL` is missing.
    - The smoke script proves schema creation/use plus write/read behavior against a live Postgres connection.
    - The runtime smoke path is callable through a documented package script.
  </acceptance_criteria>
  <done>Operators can prove the database-backed runtime flow against a real Postgres instance on demand.</done>
</task>

<task type="auto">
  <name>Task 3: Improve PDF readability without reopening export architecture</name>
  <files>src/exports/pdf-templates.js, scripts/validate-reporting.js</files>
  <read_first>.planning/phases/07-production-readiness-cleanup/07-CONTEXT.md, .planning/phases/06-real-pdf-export-delivery/06-CONTEXT.md, src/exports/pdf-templates.js, scripts/validate-reporting.js</read_first>
  <action>Refine the current executive and operational HTML templates so the stripped-text PDF output is easier to read in audit contexts: improve section naming, ordering, headings, and report metadata presentation while preserving the current renderer and export contract. Extend `scripts/validate-reporting.js` only as needed to keep these readability expectations deterministic and regression-resistant. Do not add a new PDF dependency or change the current route contract.</action>
  <verify>`node scripts/validate-reporting.js pdf-audit` exits 0 and still proves the report structure, with any new readability assertions passing.</verify>
  <acceptance_criteria>
    - PDF output remains contract-compatible with Phase 6.
    - Report headings and sections are clearer for audit readers.
    - No renderer swap or export architecture change is introduced.
  </acceptance_criteria>
  <done>The current PDF exports remain minimal but are easier to read and review.</done>
</task>

<task type="auto">
  <name>Task 4: Backfill Nyquist validation artifacts for Phases 1 through 6</name>
  <files>.planning/phases/01-observability-polling-foundation/01-VALIDATION.md, .planning/phases/02-integrity-alerting-core/02-VALIDATION.md, .planning/phases/03-reporting-compliance-governance/03-VALIDATION.md, .planning/phases/04-runtime-inventory-polling-persistence-wiring/04-VALIDATION.md, .planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-VALIDATION.md, .planning/phases/06-real-pdf-export-delivery/06-VALIDATION.md</files>
  <read_first>.planning/v1.0-MILESTONE-AUDIT.md, .planning/phases/01-observability-polling-foundation/01-VERIFICATION.md, .planning/phases/02-integrity-alerting-core/02-VERIFICATION.md, .planning/phases/03-reporting-compliance-governance/03-VERIFICATION.md, .planning/phases/04-runtime-inventory-polling-persistence-wiring/04-VERIFICATION.md, .planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-VERIFICATION.md, .planning/phases/06-real-pdf-export-delivery/06-VERIFICATION.md</read_first>
  <action>Create retroactive `*-VALIDATION.md` artifacts for every completed phase from 1 through 6, using the shipped plan, summary, and verification evidence as the source of truth. Mark what was validated, what remains residual risk, and whether Nyquist compliance is satisfied or partial for each phase without reopening already-completed requirements. The goal is to close the audit's missing-validation-artifact debt with accurate historical records.</action>
  <verify>All six `*-VALIDATION.md` files exist and the milestone audit's missing Nyquist-artifact state can be re-audited from `missing` to an evidence-based result.</verify>
  <acceptance_criteria>
    - Each completed phase 1-6 has a corresponding `*-VALIDATION.md`.
    - The files reflect shipped evidence rather than speculative future work.
    - Nyquist coverage is no longer absent project-wide after this phase executes.
  </acceptance_criteria>
  <done>The project has retroactive validation artifacts for all completed v1 phases.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-all.js` exits 0
- [ ] The package-level quality command runs build + typecheck + validations
- [ ] `DATABASE_URL=... node scripts/runtime-smoke.js` exits 0 against a real Postgres instance
- [ ] `node scripts/validate-reporting.js pdf-audit` exits 0 after the PDF readability cleanup
- [ ] `01-VALIDATION.md` through `06-VALIDATION.md` all exist
- [ ] Phase 7 does not introduce a new PDF architecture or reopen milestone feature scope
</verification>

<success_criteria>
- Production readiness is backed by a reproducible gate rather than scattered manual checks
- Runtime persistence is proven against a live Postgres database
- PDF exports are modestly more readable without architectural churn
- The project no longer lacks Nyquist validation artifacts for the completed milestone phases
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-readiness-cleanup/07-01-SUMMARY.md`
</output>
