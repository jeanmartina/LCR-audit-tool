---
phase: 23
plan: 23-04
name: Validation Proof and Documentation Closure
wave: 4
depends_on:
  - 23-01
  - 23-02
  - 23-03
requirements_addressed: [DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, SEC-01]
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - SEC-01
autonomous: true
files_modified:
  - scripts/validate-document-snapshots.js
  - scripts/validate-all.js
  - docs/operators.md
  - README.md
  - .planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-PROOF.md
---

# Phase 23 Plan 23-04: Validation Proof and Documentation Closure

**Created:** 2026-05-01  
**Status:** Ready for execution

## Objective

Close Phase 23 with full validator coverage, operator documentation for the new document snapshot path, and proof that all DOCS and SEC requirements are represented without adding OCSP, reporting UI, crawling, or AI analysis.

## Scope

### In Scope

- Complete `scripts/validate-document-snapshots.js` coverage.
- Ensure `scripts/validate-all.js` includes the Phase 23 validator.
- Add concise README/operator documentation pointers for policy-document monitoring.
- Create `23-PROOF.md` with validation commands and scope boundaries.
- Run full validation suite.

### Out of Scope

- Reporting UI and executive cards.
- OCSP polling.
- AI compliance analysis.
- Manual source management or crawling.

## Canonical References

- `.planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-CONTEXT.md`
- `.planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-VALIDATION.md`
- `.planning/REQUIREMENTS.md`
- `scripts/validate-document-snapshots.js`
- `scripts/validate-all.js`
- `README.md`
- `docs/operators.md`

## Threat Model

<threat_model>
T-23-11: Validation may pass while key requirements are missing. Mitigation: the final validator must contain and check every Phase 23 requirement ID: `DOCS-01`, `DOCS-02`, `DOCS-03`, `DOCS-04`, `DOCS-05`, and `SEC-01`.

T-23-12: Documentation may imply AI analysis or OCSP validation exists. Mitigation: README/operator docs and `23-PROOF.md` explicitly state that Phase 23 stores document evidence only; OCSP is Phase 24 and AI analysis is future scope.
</threat_model>

## Implementation Tasks

<task id="23-04-T1" title="Complete final document snapshot validator">
  <read_first>
    - `scripts/validate-document-snapshots.js`
    - `scripts/validate-all.js`
    - `.planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-VALIDATION.md`
  </read_first>
  <action>
    Extend `scripts/validate-document-snapshots.js` into the final Phase 23 gate.

    It must assert all of the following:
    - Requirement markers: `DOCS-01`, `DOCS-02`, `DOCS-03`, `DOCS-04`, `DOCS-05`, `SEC-01`.
    - Safe fetch: `assertPublicMonitoringSourceUrl`, `fetchMonitoringSourceBytes`, `MONITORING_SOURCE_MAX_DOCUMENT_BYTES`, `MONITORING_SOURCE_MAX_REDIRECTS`, private-address blocking, and public `http:`/`https:` handling.
    - Storage: `monitoring_source_events`, `document_snapshots`, `raw_body`, `content_sha256`, `extracted_text`, `extracted_text_truncated`, `extraction_status`, `metadata_json`, and trust-list provenance columns.
    - Snapshot service: `checkPolicyDocumentSource`, `extractDocumentText`, `recordMonitoringSourceEvent`, `recordDocumentSnapshot`, `findLatestDocumentSnapshotForSource`, `changed`, `unchanged`, `extraction_failed`, `blocked`, `oversized`, `unavailable`, and `not_checkable`.
    - Worker: `runScheduledDocumentSourceChecks` is loaded and called by `scripts/run-worker.js`.
    - Runtime config: `.env.example` and `compose.yaml` include required `MONITORING_SOURCE_*` keys.
    - Scope boundary: `src/monitoring-sources/worker.ts` does not process `ocsp` sources and no new manual source UI route is added under `src/app/admin`.

    Ensure `scripts/validate-all.js` contains `validate-document-snapshots.js` exactly once.
  </action>
  <acceptance_criteria>
    - `scripts/validate-document-snapshots.js` contains `DOCS-01`.
    - `scripts/validate-document-snapshots.js` contains `DOCS-05`.
    - `scripts/validate-document-snapshots.js` contains `SEC-01`.
    - `scripts/validate-document-snapshots.js` contains `assertPublicMonitoringSourceUrl`.
    - `scripts/validate-document-snapshots.js` contains `runScheduledDocumentSourceChecks`.
    - `scripts/validate-document-snapshots.js` contains `extraction_failed`.
    - `scripts/validate-all.js` contains `validate-document-snapshots.js`.
    - `node scripts/validate-document-snapshots.js` exits 0.
  </acceptance_criteria>
</task>

<task id="23-04-T2" title="Document policy document monitoring boundaries">
  <read_first>
    - `README.md`
    - `docs/operators.md`
    - `.planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-CONTEXT.md`
  </read_first>
  <action>
    Update `README.md` with a concise section named `## Policy document monitoring` that states:
    - CP/CPS/DPC document sources are derived from certificate policy pointers and existing provenance.
    - Public HTTP and HTTPS document URLs can be checked, but private/internal targets and unsafe redirects are blocked.
    - The system stores bounded raw document snapshots, hashes, metadata, extracted text when feasible, and provenance.
    - Phase 23 does not perform AI compliance analysis or OCSP validation.

    Ensure `docs/operators.md` contains `## Policy document monitoring limits` from Plan 23-03 and add a short `### Evidence and future AI boundary` subsection that states raw snapshots are retained for future analysis but no AI interpretation is performed in this milestone.
  </action>
  <acceptance_criteria>
    - `README.md` contains `## Policy document monitoring`.
    - `README.md` contains `private/internal targets`.
    - `README.md` contains `bounded raw document snapshots`.
    - `README.md` contains `does not perform AI compliance analysis`.
    - `docs/operators.md` contains `## Policy document monitoring limits`.
    - `docs/operators.md` contains `### Evidence and future AI boundary`.
  </acceptance_criteria>
</task>

<task id="23-04-T3" title="Create Phase 23 proof artifact and run full verification">
  <read_first>
    - `.planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-VALIDATION.md`
    - `.planning/REQUIREMENTS.md`
    - `scripts/validate-document-snapshots.js`
  </read_first>
  <action>
    Create `.planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-PROOF.md`.

    The proof must include:
    - A checklist mapping `DOCS-01`, `DOCS-02`, `DOCS-03`, `DOCS-04`, `DOCS-05`, and `SEC-01` to implemented files.
    - The exact validation commands:
      - `node scripts/validate-document-snapshots.js`
      - `node scripts/validate-all.js`
      - `npm run typecheck`
      - `npm run build`
    - A scope boundary stating that OCSP polling, reporting UI, manual source entry, crawling, and AI compliance interpretation remain out of scope.

    Run the full verification commands and fix Phase 23-caused failures.
  </action>
  <acceptance_criteria>
    - `.planning/phases/23-cp-cps-dpc-document-snapshot-monitoring/23-PROOF.md` exists.
    - `23-PROOF.md` contains `DOCS-01`.
    - `23-PROOF.md` contains `SEC-01`.
    - `23-PROOF.md` contains `node scripts/validate-document-snapshots.js`.
    - `23-PROOF.md` contains `npm run build`.
    - `node scripts/validate-document-snapshots.js` exits 0.
    - `node scripts/validate-all.js` exits 0.
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

## Verification Commands

```bash
node scripts/validate-document-snapshots.js
node scripts/validate-all.js
npm run typecheck
npm run build
```

## Must Haves

- Final validator covers all Phase 23 requirements and key user decisions.
- Documentation states the fetch/storage/security limits clearly.
- Proof artifact records commands and scope boundaries.
- Full suite passes before Phase 23 is considered ready for execution completion.
