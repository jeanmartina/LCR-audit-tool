---
phase: 31-dashboard-and-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/reporting/read-models.ts
  - src/reporting/timeline.ts
  - src/i18n/index.ts
  - scripts/validate-reporting.js
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Dashboard default view shows healthy and problematic items together while riskier items are listed first."
    - "Derived-source status labels are consistent across dashboard summaries, detail diagnostics, and executive reporting."
    - "Timeline entries are human-readable first, with technical evidence available on demand."
  artifacts:
    - path: "src/reporting/read-models.ts"
      provides: "Central six-state taxonomy mapping and dashboard ordering contract"
    - path: "src/reporting/timeline.ts"
      provides: "Human-first timeline event shape with expandable technical payload"
    - path: "src/i18n/index.ts"
      provides: "Localized labels/copy for six-state taxonomy and narrative timeline text"
    - path: "scripts/validate-reporting.js"
      provides: "Regression anchors for UI-05 reporting contracts"
  key_links:
    - from: "src/reporting/read-models.ts"
      to: "src/app/reporting/page.tsx"
      via: "dashboard row ordering and state labels"
      pattern: "buildDashboardRows.*normalize"
    - from: "src/reporting/timeline.ts"
      to: "src/app/reporting/[targetId]/page.tsx"
      via: "timeline event rendering"
      pattern: "summary|technical"
---

<objective>
Deliver the reporting/domain foundation for UI-05 by normalizing status taxonomy, preserving healthy visibility with risk-first ordering, and producing human-first timeline events with drill-down technical evidence.

Purpose: Implement locked decisions D-01 through D-06 in one canonical read-model layer so all reporting surfaces consume the same behavior.
Output: Updated reporting read-model/timeline contracts, localization keys, and validator coverage for UI-05.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard-and-reporting/31-CONTEXT.md
@.planning/phases/31-dashboard-and-reporting/31-RESEARCH.md
@.planning/phases/25-monitoring-source-reporting-and-executive-visibility/25-SUMMARY.md
@src/reporting/read-models.ts
@src/reporting/timeline.ts
@src/i18n/index.ts
@scripts/validate-reporting.js

<interfaces>
From src/reporting/read-models.ts:
```typescript
export interface DashboardRow { /* existing reporting row contract */ }
export interface DerivedSourceDetail { /* derived source diagnostics contract */ }
export type DerivedSourceDisplayStatus = /* current derived source labels */;
export async function buildDashboardRows(...): Promise<DashboardRow[]>;
export async function buildDashboardSummary(...): Promise<DashboardSummary>;
export async function buildExecutiveSummary(...): Promise<ExecutiveSummary>;
```

From src/reporting/timeline.ts:
```typescript
export interface TimelineEvent {
  type: TimelineEventType;
  at: Date;
  title: string;
  detail: string;
}
export async function buildAuditTimeline(...): Promise<TimelineEvent[]>;
```

From src/i18n/index.ts:
```typescript
export async function getPrincipalTranslator(...): Promise<{ t: (...) => string; locale: AppLocale }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and wire canonical UI-05 state contracts in read models</name>
  <read_first>
    - src/reporting/read-models.ts
    - .planning/phases/31-dashboard-and-reporting/31-CONTEXT.md
    - .planning/phases/31-dashboard-and-reporting/31-RESEARCH.md
  </read_first>
  <files>src/reporting/read-models.ts, src/i18n/index.ts</files>
  <acceptance_criteria>
    - Implements D-03 and D-04 with one exported six-state taxonomy (`ok`, `degraded`, `blocked`, `failed`, `not-checkable`, `unknown`) used by dashboard summary, derived-source summary, and detail diagnostics.
    - Implements D-01 and D-02 by preserving default inclusion of healthy rows and applying risk-first ordering without filtering healthy rows out.
    - Adds translation keys for each six-state label and short operator description in `en`, `pt-BR`, and `es`.
  </acceptance_criteria>
  <action>Create an exported normalization helper in `src/reporting/read-models.ts` that maps existing raw status values to the six locked UI states per D-03/D-04, then update row/summary/detail builders to emit this normalized field while preserving existing authorization and data provenance behavior. Keep dashboard default inclusion semantics intact and apply deterministic descending risk order for problematic rows first per D-01/D-02. Add localized state label keys under `reporting.state.ui.*` for all three locales in `src/i18n/index.ts`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Read-model outputs expose consistent normalized state values and translations compile with no missing-key/type errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade timeline events to human-first narrative with technical drill-down payload</name>
  <read_first>
    - src/reporting/timeline.ts
    - src/app/reporting/[targetId]/page.tsx
    - .planning/phases/31-dashboard-and-reporting/31-CONTEXT.md
  </read_first>
  <files>src/reporting/timeline.ts, src/i18n/index.ts</files>
  <acceptance_criteria>
    - Implements D-05 by emitting a human-readable narrative summary field for each timeline event type.
    - Implements D-06 by preserving or emitting structured technical evidence payload that UI can reveal with an expander.
    - Maintains existing filter behavior and date-window semantics from `parseReportFilters`.
  </acceptance_criteria>
  <action>Refactor `buildAuditTimeline` output contract in `src/reporting/timeline.ts` to include a primary narrative text and a secondary technical detail payload (for example list/array fields) so the default line reads in human language while technical depth remains available for expansion per D-05/D-06. Add/adjust localization keys for narrative phrasing in all supported locales and keep sorting/filter behavior compatible with current reporting query filters.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Timeline builder returns narrative-first events plus technical evidence payload, with typing and locale usage validated.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce UI-05 reporting foundation with validator anchors</name>
  <read_first>
    - scripts/validate-reporting.js
    - src/reporting/read-models.ts
    - src/reporting/timeline.ts
  </read_first>
  <files>scripts/validate-reporting.js</files>
  <acceptance_criteria>
    - Validator asserts existence of six-state normalization and timeline narrative/technical split anchors.
    - Validator fails if read-model reverts to inconsistent status mapping or timeline omits drill-down payload.
    - Validator remains included in `npm run validate` flow.
  </acceptance_criteria>
  <action>Update `scripts/validate-reporting.js` with durable symbol/anchor checks for UI-05 contracts: six-state normalization helper, risk-first ordering hook, and timeline narrative + technical fields. Keep assertions resilient to formatting changes while specific enough to catch regression.</action>
  <verify>
    <automated>node scripts/validate-reporting.js</automated>
  </verify>
  <done>Reporting validator enforces UI-05 foundations and passes on the updated implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> reporting query params | Untrusted filter input crosses into read-model selection |
| runtime evidence records -> rendered timeline copy | Potentially noisy technical content is transformed for UI display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | E | `src/reporting/read-models.ts` | mitigate | Preserve existing principal-scoped filtering and permission checks; no new unscoped fetch paths. |
| T-31-02 | T | `src/reporting/timeline.ts` | mitigate | Keep typed event construction and avoid HTML injection patterns; render narrative/details as plain text. |
| T-31-03 | I | `src/reporting/read-models.ts` | mitigate | Normalize only operator-facing states and avoid exposing hidden raw internals not already visible in authorized detail pages. |
</threat_model>

<verification>
Run:
- `npm run typecheck`
- `node scripts/validate-reporting.js`
- `npm run validate`
</verification>

<success_criteria>
- UI-05 foundational contracts exist in read models and timeline builders.
- Locked decisions D-01 to D-06 are implemented in code paths consumed by reporting pages.
- Validator coverage fails on regression and passes on final implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-and-reporting/31-01-SUMMARY.md`
</output>

