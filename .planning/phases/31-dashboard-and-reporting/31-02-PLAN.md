---
phase: 31-dashboard-and-reporting
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/app/reporting/page.tsx
  - src/app/reporting/[targetId]/page.tsx
  - src/app/reporting/executive/page.tsx
  - src/components/ui/primitives.tsx
  - src/i18n/index.ts
  - scripts/validate-reporting.js
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Operators can see healthy and problematic items in one default dashboard view with problematic rows emphasized first."
    - "Actions are presented as consistent controls with a stable hierarchy across dashboard and detail pages."
    - "Detail reporting acts as a diagnostic page with human-readable timelines and expandable technical evidence."
  artifacts:
    - path: "src/app/reporting/page.tsx"
      provides: "Risk-prioritized dashboard with fixed global actions and inline contextual actions"
    - path: "src/app/reporting/[targetId]/page.tsx"
      provides: "Diagnostic-first detail page with narrative timeline and expandable technical evidence"
    - path: "src/app/reporting/executive/page.tsx"
      provides: "Executive surface consuming consistent normalized state taxonomy"
    - path: "src/components/ui/primitives.tsx"
      provides: "Consistent action/control presentation primitives reused by reporting pages"
  key_links:
    - from: "src/app/reporting/page.tsx"
      to: "src/reporting/read-models.ts"
      via: "normalized status and risk-first dashboard rows"
      pattern: "buildDashboardRows"
    - from: "src/app/reporting/[targetId]/page.tsx"
      to: "src/reporting/timeline.ts"
      via: "narrative timeline rendering with expander"
      pattern: "buildAuditTimeline"
    - from: "src/app/reporting/executive/page.tsx"
      to: "src/reporting/read-models.ts"
      via: "executive state summaries"
      pattern: "buildExecutiveSummary"
---

<objective>
Apply UI-05 to reporting pages by wiring normalized status/timeline contracts into dashboard/detail/executive UI with consistent global and contextual action controls.

Purpose: Implement locked decisions D-07 and D-08 while completing end-user visibility outcomes for D-01 through D-06.
Output: Updated reporting pages and UI primitives that render consistent action hierarchy, readable states, and diagnostic-first detail behavior.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard-and-reporting/31-CONTEXT.md
@.planning/phases/31-dashboard-and-reporting/31-RESEARCH.md
@.planning/phases/31-dashboard-and-reporting/31-UI-SPEC.md
@.planning/phases/31-dashboard-and-reporting/31-01-PLAN.md
@src/app/reporting/page.tsx
@src/app/reporting/[targetId]/page.tsx
@src/app/reporting/executive/page.tsx
@src/components/ui/primitives.tsx

<interfaces>
From src/reporting/query-state.ts:
```typescript
export interface ReportFilters { /* mode, tab, period, filters */ }
export function parseReportFilters(searchParams: SearchParamLike): ReportFilters;
export function withFilter(filters: ReportFilters, patch: Partial<ReportFilters>): string;
```

From src/components/ui/primitives.tsx:
```typescript
export function PageHeader(...): ReactElement;
export function ActionButton(...): ReactElement;
export function StatusPill(...): ReactElement;
export function Panel(...): ReactElement;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Standardize reporting action controls and hierarchy</name>
  <read_first>
    - src/components/ui/primitives.tsx
    - src/app/reporting/page.tsx
    - src/app/reporting/[targetId]/page.tsx
    - .planning/phases/31-dashboard-and-reporting/31-UI-SPEC.md
  </read_first>
  <files>src/components/ui/primitives.tsx, src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx, src/i18n/index.ts</files>
  <acceptance_criteria>
    - Implements D-07 by rendering fixed global primary actions at the reporting page level and contextual inline actions per row/detail section.
    - Implements D-08 with one consistent action style hierarchy across dashboard and detail pages.
    - Primary action text and empty/error copy align with 31-UI-SPEC contract.
  </acceptance_criteria>
  <action>Refactor reporting action UI to use shared primitives with explicit hierarchy: one page-level primary control block (for example executive/open/export actions) and inline contextual controls on rows/detail sections per D-07. Ensure button/link visual treatment is consistent between `/reporting` and `/reporting/[targetId]` per D-08, and wire localized copy keys using the approved UI-SPEC phrases where applicable.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Reporting actions are visually and structurally consistent, with page-level primary and inline contextual controls both present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert reporting dashboard and detail into diagnostic-first surfaces</name>
  <read_first>
    - src/app/reporting/page.tsx
    - src/app/reporting/[targetId]/page.tsx
    - src/reporting/read-models.ts
    - src/reporting/timeline.ts
  </read_first>
  <files>src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx, src/app/reporting/executive/page.tsx, src/i18n/index.ts</files>
  <acceptance_criteria>
    - Dashboard default keeps healthy rows visible while problematic rows appear first (D-01/D-02 behavior visible in UI).
    - Detail page shows human-readable timeline entries first with expandable technical evidence per event (D-05/D-06).
    - Derived-state labels on dashboard/detail/executive consume the same six-state taxonomy from Plan 31-01 (D-03/D-04).
  </acceptance_criteria>
  <action>Update the reporting pages to consume Plan 31-01 normalized read-model and timeline fields directly: dashboard ordering/labels from normalized rows, detail timeline narrative plus expander for technical evidence, and executive badges/cards from the same taxonomy mapping. Preserve existing auth and group scoping, and keep filter parsing/serialization behavior unchanged.</action>
  <verify>
    <automated>npm run build && npm run typecheck</automated>
  </verify>
  <done>Dashboard/detail/executive pages render consistent taxonomy and diagnostic behaviors without regression in build/typecheck.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize UI-05 regression anchors and full quality verification</name>
  <read_first>
    - scripts/validate-reporting.js
    - src/app/reporting/page.tsx
    - src/app/reporting/[targetId]/page.tsx
    - src/app/reporting/executive/page.tsx
  </read_first>
  <files>scripts/validate-reporting.js</files>
  <acceptance_criteria>
    - Validator covers UI anchors for global+contextual action pattern and diagnostic timeline expansion behavior.
    - Validation catches taxonomy/rendering regressions across dashboard/detail/executive surfaces.
    - Full project quality command passes.
  </acceptance_criteria>
  <action>Extend `scripts/validate-reporting.js` to assert D-07/D-08 UI anchors in reporting pages and confirm these pages consume normalized/timeline contracts from Plan 31-01. Keep checks deterministic and maintainable, then run full quality gate.</action>
  <verify>
    <automated>npm run quality</automated>
  </verify>
  <done>Validator and full quality suite pass with UI-05 behavioral anchors enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user interaction -> action controls | User-triggered actions must remain scoped to authorized reporting routes |
| detail evidence -> UI expander | Technical evidence is exposed to authorized users and must stay text-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | S | reporting action controls | mitigate | Keep action targets on existing authenticated reporting routes and reuse existing permission checks. |
| T-31-05 | I | detail diagnostics timeline | mitigate | Keep evidence access bound to current `detail.view` authorization path; do not fetch unscoped records. |
| T-31-06 | T | page rendering of evidence fields | mitigate | Render narrative/technical content as plain text through React; avoid unsafe HTML rendering. |
</threat_model>

<verification>
Run:
- `npm run build`
- `npm run typecheck`
- `node scripts/validate-reporting.js`
- `npm run quality`
</verification>

<success_criteria>
- Locked decisions D-01 through D-08 are visible in reporting UI behavior.
- UI-05 outcomes are delivered without changing monitoring collection scope.
- Dashboard/detail/executive surfaces present consistent states, actions, and diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-and-reporting/31-02-SUMMARY.md`
</output>

