---
phase: 09-certificate-first-onboarding-and-target-administration
plan: 01
type: execute
wave: 1
depends_on: [08-01]
files_modified: [src/storage/runtime-store.ts, src/inventory/, src/app/, src/auth/, src/reporting/, scripts/validate-onboarding-admin.js]
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, GRP-05, GRP-06]
user_setup:
  - sample certificates or a representative `.zip` archive are useful to exercise the import paths realistically
must_haves:
  truths:
    - "The certificate is the primary admin object; derived CRLs are operational results, not the primary thing users create."
    - "Manual CRL URL entry must not remain the normal onboarding path."
    - "Runtime CRL execution may be deduplicated, but visibility stays certificate/group-scoped."
    - "Shared certificates require group-specific overrides and effective-value previews before save."
  artifacts:
    - src/storage/runtime-store.ts
    - src/inventory/
    - src/app/
    - scripts/validate-onboarding-admin.js
  key_links:
    - .planning/phases/09-certificate-first-onboarding-and-target-administration/09-CONTEXT.md
    - .planning/phases/09-certificate-first-onboarding-and-target-administration/09-RESEARCH.md
    - .planning/REQUIREMENTS.md
---
<objective>
Build the certificate-first onboarding and administration layer for v1.1 so operators can import certificates, derive CRLs automatically, share certificates across groups with overrides, and manage this state from product surfaces instead of SQL.
Purpose: Phase 8 established auth and permission boundaries; Phase 9 must now create the real operator/admin model that later reporting, i18n, and packaging work will sit on top of.
Output: durable certificate/admin persistence, single and batch import flows, certificate admin read/write surfaces, and validation proving certificate-first onboarding replaced manual target entry as the primary operator path.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add certificate-centric persistence, sharing, overrides, templates, and change history</name>
  <files>src/storage/runtime-store.ts, src/inventory/targets.ts, src/inventory/target-admin.ts, scripts/validate-onboarding-admin.js</files>
  <read_first>.planning/phases/09-certificate-first-onboarding-and-target-administration/09-CONTEXT.md, .planning/phases/09-certificate-first-onboarding-and-target-administration/09-RESEARCH.md, .planning/REQUIREMENTS.md, src/storage/runtime-store.ts</read_first>
  <action>Extend the Postgres-backed source-of-truth model so the product can persist certificate-first administration state separately from the runtime polling projection. Add entities and helpers for certificates, import runs/items, derived CRL links with ignored-url support, certificate-to-group sharing, group-specific overrides, template records, and change history/audit events. Keep enough projection/update hooks so runtime targets can still be produced for existing polling/reporting code without making runtime targets the only admin abstraction.</action>
  <verify>`node scripts/validate-onboarding-admin.js schema` exits 0 and prints `Certificate admin schema ready`.</verify>
  <acceptance_criteria>
    - Persistence model supports single certificate identity by stable fingerprint.
    - Shared certificates and per-group overrides can be represented durably.
    - Derived CRL links exist and can be flagged as ignored.
    - Template and change-history storage exist for downstream admin UX.
    - Validation script is introduced in this task and asserts the required persistence/admin artifacts.
  </acceptance_criteria>
  <done>The codebase has a real certificate administration model instead of only a target runtime table.</done>
</task>

<task type="auto">
  <name>Task 2: Implement single-certificate and `.zip` import flows with fingerprint dedup and CRL derivation</name>
  <files>src/inventory/, src/app/api/, src/app/, src/storage/runtime-store.ts, scripts/validate-onboarding-admin.js</files>
  <read_first>.planning/phases/09-certificate-first-onboarding-and-target-administration/09-CONTEXT.md, .planning/phases/09-certificate-first-onboarding-and-target-administration/09-RESEARCH.md, src/inventory/targets.ts, src/inventory/target-admin.ts, src/auth/authorization.ts</read_first>
  <action>Build the certificate ingestion pipeline for both single uploads and separate batch `.zip` imports. Derive CRL URLs from uploaded certificates, deduplicate certificates by fingerprint, update on re-import by default, preserve item-level batch results, and reuse runtime CRL execution records internally when the same public CRL URL already exists. Ensure no primary operator path depends on manual CRL URL entry. Protect all onboarding actions with the Phase 8 permission model.</action>
  <verify>`node scripts/validate-onboarding-admin.js import` exits 0 and prints `Certificate import flows ready`.</verify>
  <acceptance_criteria>
    - Single certificate upload exists and derives CRL targets automatically.
    - Batch `.zip` import exists as a separate flow and supports partial success reporting.
    - Known certificates are updated/reprocessed by default rather than blindly duplicated.
    - Runtime reuse for duplicate CRL URLs exists without weakening group authorization.
    - Manual CRL URL entry is no longer the normal onboarding path exposed to operators.
  </acceptance_criteria>
  <done>Operators can onboard domain assets through certificates instead of SQL or raw CRL URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Deliver the certificate administration UX with effective-value previews, sharing, templates, and history</name>
  <files>src/app/, src/reporting/, src/inventory/, src/auth/, scripts/validate-onboarding-admin.js</files>
  <read_first>.planning/phases/09-certificate-first-onboarding-and-target-administration/09-CONTEXT.md, .planning/phases/09-certificate-first-onboarding-and-target-administration/09-RESEARCH.md, src/app/reporting/page.tsx, src/auth/authorization.ts</read_first>
  <action>Create the operator-facing admin surfaces for certificate list, certificate detail/edit, and batch import. The list should expose search/filtering, status, linked groups, derived CRL count, last import, and quick actions. The create/edit/detail flows must show derived CRL preview, ignored URL controls, effective configuration/default previews, effective alert-recipient preview, sharing controls across groups, full change history, manual validation/connectivity test entry points, and template-based clone behavior. Keep enforcement on backend/read-model/server paths, not only UI conditions.</action>
  <verify>`node scripts/validate-onboarding-admin.js ui` exits 0 and prints `Certificate admin UI ready`.</verify>
  <acceptance_criteria>
    - The admin UX centers on certificates, not raw CRL targets.
    - Shared certificates can be administered with group-specific overrides.
    - Effective resolved values are visible before save.
    - Clone creates a template-oriented path rather than a live duplicate monitoring object.
    - Change history includes who changed what and when, not only a shallow diff.
  </acceptance_criteria>
  <done>The product now has a real certificate-first admin surface that non-developers can use.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-onboarding-admin.js schema` exits 0
- [ ] `node scripts/validate-onboarding-admin.js import` exits 0
- [ ] `node scripts/validate-onboarding-admin.js ui` exits 0
- [ ] Single-certificate onboarding derives CRLs without manual-primary CRL URL entry
- [ ] Batch `.zip` import supports partial success and dedup/update behavior
- [ ] Shared certificates support group-specific overrides
- [ ] Runtime CRL execution reuse does not bypass group visibility rules
- [ ] Admin UI exposes effective values, ignored derived URLs, templates, and full change history
</verification>

<success_criteria>
- Operators can onboard single certificates and `.zip` bundles without SQL
- Certificate administration becomes the primary operator surface for monitored assets
- Shared certificates and per-group overrides are supported cleanly
- The codebase is ready for Phase 10 to extend reporting and predictive behavior on top of the new admin model
</success_criteria>

<output>
After completion, create `.planning/phases/09-certificate-first-onboarding-and-target-administration/09-01-SUMMARY.md`
</output>
