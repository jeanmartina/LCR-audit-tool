---
phase: 05-reporting-e2e-wiring-audit-ux-completion
plan: 01
type: execute
wave: 1
depends_on: [04-01]
files_modified: [src/reporting/read-models.ts, src/reporting/timeline.ts, src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx, src/exports/csv.ts, src/exports/pdf.ts, scripts/validate-reporting.js]
autonomous: true
requirements: [MON-02, ALT-02, REP-01, REP-02]
user_setup: []
must_haves:
  truths:
    - "Reporting must consume the persisted runtime evidence path closed in Phase 4 rather than relying on placeholder-only reporting behavior."
    - "Dashboard and drill-down pages must share one reproducible filter contract with the export actions."
    - "Dashboard filters auto-apply, while drill-down filters use explicit Apply and Clear actions."
    - "The target detail experience must present a fixed summary and tabbed audit evidence with Timeline as the default tab."
    - "Exports must be reachable from the UI and reflect the exact filters visible on the current screen."
  artifacts:
    - src/reporting/read-models.ts
    - src/reporting/timeline.ts
    - src/app/reporting/page.tsx
    - src/app/reporting/[targetId]/page.tsx
    - src/exports/csv.ts
    - src/exports/pdf.ts
    - scripts/validate-reporting.js
  key_links:
    - src/app/reporting/page.tsx -> src/reporting/read-models.ts
    - src/app/reporting/[targetId]/page.tsx -> src/reporting/read-models.ts
    - src/app/reporting/[targetId]/page.tsx -> src/reporting/timeline.ts
    - src/app/reporting/page.tsx -> src/exports/csv.ts
    - src/app/reporting/page.tsx -> src/exports/pdf.ts
    - src/app/reporting/[targetId]/page.tsx -> src/exports/csv.ts
    - src/app/reporting/[targetId]/page.tsx -> src/exports/pdf.ts
---
<objective>
Close the reporting gap identified by the milestone audit by wiring the dashboard, drill-down, SLA summary, and export flows to the persisted runtime evidence introduced in Phase 4.
Purpose: Phase 5 must complete the monitoring → validation/alerting → reporting flow so compliance evidence is queryable, filterable, and exportable from the real runtime data path.
Output: Shared reporting filter contract, aggregate SLA summary, functional dashboard/drill-down UX, reachable CSV/PDF exports, and stronger reporting verification.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Establish shared reporting query contract and selected-period read models</name>
  <files>src/reporting/read-models.ts, src/reporting/timeline.ts, scripts/validate-reporting.js</files>
  <read_first>.planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-CONTEXT.md, .planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-RESEARCH.md, src/reporting/read-models.ts, src/reporting/timeline.ts</read_first>
  <action>Extend the reporting read-model layer so dashboard, detail, timeline, and export flows all share one filter contract that covers source, issuer, criticality, owner, status, date range, HTTP status, severity, event type, and snapshot/hash filtering as required by the Phase 5 context. Add selected-period aggregate summary data for the dashboard (for example target counts by state, average SLA, open alerts, and upcoming expirations) without moving this aggregation logic into the page component. Update `src/reporting/timeline.ts` so tabbed audit consumption can filter timeline entries by the shared detail filter state. Upgrade `scripts/validate-reporting.js` in this task so later UI/export work can be validated against the real contract rather than placeholder strings.</action>
  <verify>`node scripts/validate-reporting.js read-models` exits 0 and prints `Reporting read models ready`.</verify>
  <acceptance_criteria>
    - The reporting filter contract covers both dashboard and drill-down filter fields from `05-CONTEXT.md`.
    - Dashboard read models expose selected-period aggregate summary data in addition to per-target rows.
    - Detail and timeline read-models can honor explicit audit filters without duplicating filter logic in the page layer.
    - `scripts/validate-reporting.js` checks behavior-oriented reporting contracts instead of relying on placeholder-only assertions.
  </acceptance_criteria>
  <done>Reporting pages and exports have one shared read/query contract over runtime evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the dashboard to functional filters, aggregate summary, and screen-level exports</name>
  <files>src/app/reporting/page.tsx, src/reporting/read-models.ts, src/exports/csv.ts, src/exports/pdf.ts, scripts/validate-reporting.js</files>
  <read_first>.planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-CONTEXT.md, src/app/reporting/page.tsx, src/reporting/read-models.ts, src/exports/csv.ts, src/exports/pdf.ts</read_first>
  <action>Replace the placeholder dashboard filters in `src/app/reporting/page.tsx` with functional controls for source, issuer, criticality, owner, status, and period. Make them auto-apply through reproducible query-string state, render the selected-period aggregate summary above the table, and ensure the table rows and summary are both derived from the shared reporting read-model contract over runtime evidence. Connect the dashboard screen to its export actions so users can trigger the filtered-table CSV and executive PDF from the current screen state, with the export payloads using the same filters visible in the UI.</action>
  <verify>`node scripts/validate-reporting.js dashboard` exits 0 and prints `Reporting dashboard wired`.</verify>
  <acceptance_criteria>
    - Dashboard filters are no longer placeholders and are encoded in screen state/query parameters.
    - The dashboard shows a selected-period summary plus per-target rows sourced from runtime evidence.
    - Dashboard export actions are reachable from the UI and preserve the currently visible filters.
    - The dashboard satisfies the selected-period SLA and reporting requirements from `ALT-02` and `REP-01`.
  </acceptance_criteria>
  <done>Compliance users can filter the main dashboard and export exactly what they are looking at.</done>
</task>

<task type="auto">
  <name>Task 3: Deliver the audit drill-down with summary, tabs, explicit filters, and target-scoped exports</name>
  <files>src/app/reporting/[targetId]/page.tsx, src/reporting/read-models.ts, src/reporting/timeline.ts, src/exports/csv.ts, src/exports/pdf.ts, scripts/validate-reporting.js</files>
  <read_first>.planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-CONTEXT.md, src/app/reporting/[targetId]/page.tsx, src/reporting/timeline.ts, src/exports/csv.ts, src/exports/pdf.ts</read_first>
  <action>Refactor the target detail page into the agreed audit layout: a fixed summary block at the top followed by tabbed evidence navigation with `Timeline` as the default tab and tabs for Polls, Coverage gaps, Alerts, Validation, and Snapshots. Replace placeholder evidence filters with explicit `Apply` / `Clear` controls that drive query-string state and feed the shared read-model contract. Connect target-scoped exports so the current evidence scope can download poll, coverage-gap, alert, and snapshot CSVs plus the operational PDF using the exact active filters.</action>
  <verify>`node scripts/validate-reporting.js detail` exits 0 and prints `Reporting detail wired`.</verify>
  <acceptance_criteria>
    - The detail page starts with a fixed summary block showing current status, latest incident, SLA in the selected period, next expiration, and open alerts.
    - Evidence navigation uses tabs and defaults to `Timeline`.
    - Drill-down filters use explicit `Apply` and `Clear` behavior and operate on runtime evidence.
    - Target-scoped CSV/PDF exports are reachable from the detail screen and preserve active filters.
  </acceptance_criteria>
  <done>Auditors can reconstruct a target incident path, filter the evidence, and export the exact scoped record set.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/validate-reporting.js read-models` exits 0
- [ ] `node scripts/validate-reporting.js dashboard` exits 0
- [ ] `node scripts/validate-reporting.js detail` exits 0
- [ ] Reporting filters are functional and reproducible through shared screen/query state
- [ ] Dashboard summary and SLA values reflect the selected period over runtime evidence
- [ ] Detail page uses summary + tabbed audit navigation with Timeline as the default tab
- [ ] UI export actions are connected and preserve the currently visible filters
</verification>

<success_criteria>
- Dashboard and drill-down reporting consume the real runtime evidence path
- SLA and coverage reporting are queryable by selected period and visible to compliance users
- Audit evidence is navigable through tabs and exportable from the screen currently in use
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-e2e-wiring-audit-ux-completion/05-01-SUMMARY.md`
</output>
