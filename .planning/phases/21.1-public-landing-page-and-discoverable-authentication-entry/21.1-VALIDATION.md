---
phase: 21.1-public-landing-page-and-discoverable-authentication-entry
validated: 2026-04-24
nyquist_compliant: true
wave_0_complete: true
status: passed
---

# Phase 21.1 Validation

## Evidence Reviewed

- `21.1-01-SUMMARY.md`
- `21.1-VERIFICATION.md`
- `scripts/validate-auth-foundation.js`
- `scripts/validate-all.js`
- `src/app/page.tsx`
- `src/app/auth/page.tsx`
- `src/app/auth/accept-invite/page.tsx`
- `src/i18n/index.ts`
- `README.md`
- `docs/operators.md`

## Automated Coverage

| Coverage Area | Command | Result |
|---|---|---|
| Published root/auth entry surface | `node scripts/validate-auth-foundation.js entry` | passed |
| Project-wide regression coverage | `node scripts/validate-all.js` | passed |
| Type-level integration | `npm run typecheck` | passed |
| Production build integration | `npm run build` | passed |

## Validation Result

- The phase now has direct validator-backed coverage for the published root route, authenticated redirect behavior, `/auth` discoverability, invite/provider entry anchoring, and i18n presence for the new landing strings.
- `scripts/validate-auth-foundation.js` is the active Nyquist mechanism for `UX-08`; it checks the session-aware root route, root-to-auth navigation, provider invite links, external-provider helper usage, and localized landing/auth guidance.
- `node scripts/validate-all.js`, `npm run typecheck`, and `npm run build` confirm the new root surface integrates without regressions across the broader product.

## Requirement Mapping

| Requirement | Validation Basis | Status |
|---|---|---|
| `UX-08` | auth-entry validator checks the session-aware root route, discoverable `/auth` entry, provider visibility/action wiring, and i18n coverage | covered |

## Residual Risk

- This validation pass is code-backed and build-backed, not a live browser check against a running public deployment in this session.
- Milestone closure should still prefer one quick Docker/staging sanity check on `/`, `/auth`, and `/auth/accept-invite#provider-...` before final archival.
