# Phase 21.1 Plan 21.1-01 Summary

**Completed:** 2026-04-24  
**Code commit:** `bb1cad7` (`feat(21.1): add published auth entry`)

## Delivered

- Added a session-aware root route in `src/app/page.tsx` so published deployments no longer return a 404 at `/`.
- Redirected authenticated users from `/` to `/reporting` while rendering a public landing page for unauthenticated users.
- Kept local username/password login on `/auth`, but made the page clearly part of the main published entry flow.
- Exposed enabled and disabled identity providers on both `/` and `/auth`, with actionable invite-flow entry points only for configured providers.
- Added provider anchors on the invite page and extended auth validation coverage for the new entry surface.
- Localized the new landing/auth guidance in `en`, `pt-BR`, and `es`.

## Verification

- `node scripts/validate-auth-foundation.js entry`
- `npm run typecheck`
- `npm run build`
- `node scripts/validate-all.js`
