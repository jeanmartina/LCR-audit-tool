---
phase: 24
plan: 24-04
name: OCSP Validation, Documentation, and Proof Closure
wave: 4
depends_on:
  - 24-01
  - 24-02
  - 24-03
requirements_addressed: [OCSP-01, OCSP-02, OCSP-03, OCSP-04]
requirements:
  - OCSP-01
  - OCSP-02
  - OCSP-03
  - OCSP-04
autonomous: true
files_modified:
  - scripts/validate-ocsp-monitoring.js
  - README.md
  - docs/operators.md
  - .planning/phases/24-ocsp-technical-evidence-monitoring/24-PROOF.md
---

# Phase 24 Plan 24-04: OCSP Validation, Documentation, and Proof Closure

**Created:** 2026-05-04  
**Status:** Ready for execution

## Objective

Close Phase 24 with validator coverage, operator documentation, and proof that OCSP technical evidence is captured without claiming semantic revocation validation.

## Scope

### In Scope

- Final OCSP validator coverage.
- README/operator documentation of OCSP technical boundary.
- Phase proof artifact mapping requirements to files and commands.
- Full validation suite.

### Out of Scope

- Reporting UI.
- Semantic OCSP validation.
- Manual source entry.

## Canonical References

- `.planning/phases/24-ocsp-technical-evidence-monitoring/24-CONTEXT.md`
- `.planning/phases/24-ocsp-technical-evidence-monitoring/24-VALIDATION.md`
- `.planning/REQUIREMENTS.md`
- `scripts/validate-ocsp-monitoring.js`
- `README.md`
- `docs/operators.md`

## Threat Model

<threat_model>
T-24-08: Phase 24 can appear complete while plans miss the semantic-boundary requirement. Mitigation: final validator checks all OCSP requirement markers and forbidden semantic health assignments.

T-24-09: Documentation can imply OCSP reachability equals revocation coverage. Mitigation: README, operators guide, and proof must explicitly state this is technical evidence only.
</threat_model>

## Implementation Tasks

<task id="24-04-T1" title="Complete final OCSP validator">
  <read_first>
    - `scripts/validate-ocsp-monitoring.js`
    - `scripts/validate-all.js`
    - `.planning/phases/24-ocsp-technical-evidence-monitoring/24-VALIDATION.md`
  </read_first>
  <action>
    Extend `scripts/validate-ocsp-monitoring.js` into the final Phase 24 gate.

    It must assert all of the following:
    - Requirement markers `OCSP-01`, `OCSP-02`, `OCSP-03`, and `OCSP-04` are present in the validator.
    - `package.json` contains `pkijs` and `asn1js`.
    - `src/monitoring-sources/ocsp-types.ts` exists and contains all six technical event statuses.
    - `src/monitoring-sources/ocsp-types.ts` does not contain semantic health status values in `OcspCheckEventStatus`.
    - `src/storage/runtime-store.ts` contains `ocsp_check_events`, `ocsp_response_evidence`, `request_body`, `response_body`, `request_sha256`, `response_sha256`, `issuer_fingerprint`, and `metadata_json`.
    - `src/monitoring-sources/ocsp.ts` contains `checkOcspSource`, `buildOcspRequestDer`, `issuer-certificate-not-found`, `application/ocsp-request`, `application/ocsp-response`, `recordOcspCheckEvent`, and `recordOcspResponseEvidence`.
    - `src/monitoring-sources/worker.ts` contains `runScheduledOcspSourceChecks` and filters `source.sourceType === "ocsp"`.
    - `.env.example` and `compose.yaml` contain all five OCSP env keys.
    - `scripts/validate-all.js` contains `validate-ocsp-monitoring.js` exactly once.
    - `src/monitoring-sources/ocsp.ts` does not contain `status: "good"`, `status: "revoked"`, or `status: "unknown"`.
  </action>
  <acceptance_criteria>
    - `scripts/validate-ocsp-monitoring.js` contains `OCSP-04`.
    - `scripts/validate-ocsp-monitoring.js` contains `validate-ocsp-monitoring.js exactly once` or `validateAllHits.length === 1`.
    - `scripts/validate-ocsp-monitoring.js` contains `status: "good"`.
    - `node scripts/validate-ocsp-monitoring.js` exits 0 after this plan is complete.
  </acceptance_criteria>
</task>

<task id="24-04-T2" title="Document OCSP technical boundary">
  <read_first>
    - `README.md`
    - `docs/operators.md`
    - `.planning/phases/24-ocsp-technical-evidence-monitoring/24-CONTEXT.md`
  </read_first>
  <action>
    Add `## OCSP technical monitoring` to `README.md`.

    The section must state:
    - The worker sends real OCSP requests when issuer context exists.
    - Missing issuer context is recorded as `not_checkable`.
    - Raw request and response evidence is retained within configured byte limits.
    - This milestone does not perform full semantic OCSP validation.
    - `good`, `revoked`, and `unknown` are not compliance health labels in this phase.

    Ensure `docs/operators.md` has `## OCSP technical monitoring limits` and contains the same boundary language.
  </action>
  <acceptance_criteria>
    - `README.md` contains `## OCSP technical monitoring`.
    - `README.md` contains `real OCSP requests`.
    - `README.md` contains `not_checkable`.
    - `README.md` contains `does not perform full semantic OCSP validation`.
    - `README.md` contains `not compliance health labels`.
    - `docs/operators.md` contains `## OCSP technical monitoring limits`.
  </acceptance_criteria>
</task>

<task id="24-04-T3" title="Create Phase 24 proof and run full verification">
  <read_first>
    - `.planning/phases/24-ocsp-technical-evidence-monitoring/24-VALIDATION.md`
    - `.planning/REQUIREMENTS.md`
    - `scripts/validate-ocsp-monitoring.js`
  </read_first>
  <action>
    Create `.planning/phases/24-ocsp-technical-evidence-monitoring/24-PROOF.md`.

    The proof must include:
    - A checklist mapping `OCSP-01`, `OCSP-02`, `OCSP-03`, and `OCSP-04` to implemented files.
    - The exact validation commands:
      - `node scripts/validate-ocsp-monitoring.js`
      - `node scripts/validate-all.js`
      - `npm run typecheck`
      - `npm run build`
    - A scope boundary stating that semantic OCSP signature/status/freshness validation, reporting UI, and manual source entry remain out of scope.

    Run the full verification commands and fix Phase 24-caused failures.
  </action>
  <acceptance_criteria>
    - `.planning/phases/24-ocsp-technical-evidence-monitoring/24-PROOF.md` exists.
    - `24-PROOF.md` contains `OCSP-01`.
    - `24-PROOF.md` contains `OCSP-04`.
    - `24-PROOF.md` contains `node scripts/validate-ocsp-monitoring.js`.
    - `24-PROOF.md` contains `npm run build`.
    - `node scripts/validate-ocsp-monitoring.js` exits 0.
    - `node scripts/validate-all.js` exits 0.
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

## Verification Commands

```bash
node scripts/validate-ocsp-monitoring.js
node scripts/validate-all.js
npm run typecheck
npm run build
```

## Must Haves

- Final proof covers every OCSP requirement.
- Documentation clearly separates technical OCSP evidence from full revocation validation.
- Full suite passes before Phase 24 execution is considered complete.
