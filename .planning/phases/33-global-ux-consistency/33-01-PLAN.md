---
phase: 33-global-ux-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/primitives.tsx
  - src/components/public-shell.tsx
  - src/app/layout.tsx
  - src/i18n/index.ts
  - scripts/validate-ui-guidance.js
autonomous: true
requirements: [UI-07]
must_haves:
  truths:
    - "Operators encounter one compact density rhythm across top-level shells and shared UI primitives."
    - "Action controls visibly follow one hierarchy: primary solid, secondary outline, tertiary link."
    - "Hybrid navigation is explicit: global top navigation with local area sub-navigation support."
    - "Empty and error states consistently show short message, primary action, and recovery link."
  artifacts:
    - path: "src/components/ui/primitives.tsx"
      provides: "Shared UX contract primitives for density, action hierarchy, and empty/error composition"
    - path: "src/components/public-shell.tsx"
      provides: "Global top navigation contract aligned to shared action hierarchy"
    - path: "src/app/layout.tsx"
      provides: "App-wide shell-level consistency hooks/tokens for compact density"
    - path: "scripts/validate-ui-guidance.js"
      provides: "Automated UI-07 regression anchors for D-01 through D-04"
  key_links:
    - from: "src/components/ui/primitives.tsx"
      to: "src/app/reporting/page.tsx"
      via: "shared action and empty/error primitives"
      pattern: "ActionLink|ActionGroup|EmptyState|Notice"
    - from: "src/components/public-shell.tsx"
      to: "src/app/layout.tsx"
      via: "global navigation composition"
      pattern: "nav|header"
    - from: "scripts/validate-ui-guidance.js"
      to: "src/components/ui/primitives.tsx"
      via: "contract enforcement checks"
      pattern: "primary|secondary|context|EmptyState|Notice"
---

<objective>
Define and enforce the global UX consistency contracts required by UI-07 before route-level rollout.

Purpose: Implement locked decisions D-01, D-02, D-03, and D-04 as shared contracts so all major screens can consume one visual and navigation system.
Output: Updated shared primitives/shell plus validator anchors that encode compact density, action hierarchy, hybrid navigation, and empty/error structure.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-global-ux-consistency/33-CONTEXT.md
@.planning/phases/33-global-ux-consistency/33-RESEARCH.md
@.planning/phases/31-dashboard-and-reporting/31-02-SUMMARY.md
@src/components/ui/primitives.tsx
@src/components/public-shell.tsx
@src/app/layout.tsx
@scripts/validate-ui-guidance.js

<interfaces>
From src/components/ui/primitives.tsx:
```typescript
export function PageShell({ children }: { children: ReactNode }): ReactElement;
export function PageHeader(...): ReactElement;
export function ActionGroup({ children }: { children: ReactNode }): ReactElement;
export function ActionLink({ href, children, tone = "secondary" }: { href: string; children: ReactNode; tone?: "primary" | "secondary" | "context" }): ReactElement;
export function Notice(...): ReactElement;
export function EmptyState({ title, children }: { title: string; children: ReactNode }): ReactElement;
```

From src/components/public-shell.tsx:
```typescript
export interface PublicShellAction { href: string; label: string; kind?: "primary" | "secondary"; }
export interface PublicShellProps { ... }
export function PublicShell(props: PublicShellProps): ReactElement;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode shared UX contracts in primitives and shell per D-01/D-02/D-03/D-04</name>
  <read_first>
    - .planning/phases/33-global-ux-consistency/33-CONTEXT.md
    - src/components/ui/primitives.tsx
    - src/components/public-shell.tsx
    - src/app/layout.tsx
  </read_first>
  <files>src/components/ui/primitives.tsx, src/components/public-shell.tsx, src/app/layout.tsx, src/i18n/index.ts</files>
  <acceptance_criteria>
    - D-01: shared spacing/density tokens in primitives and shell preserve compact technical throughput without per-page ad-hoc overrides.
    - D-02: action hierarchy is explicit and documented in component behavior (primary solid, secondary outline, tertiary link/context).
    - D-03: global top navigation remains in shell and supports local sub-navigation hooks consumed by area pages.
    - D-04: Empty/error contract supports short message + primary action + recovery link using shared primitives and i18n labels.
  </acceptance_criteria>
  <action>Refactor shared UI contracts so all route surfaces can adopt one consistency baseline: tighten and centralize compact spacing tokens (D-01), enforce action tone semantics in `ActionLink`/related primitives (D-02), preserve and normalize global-top navigation affordances in shell/layout with local-subnav compatibility points (D-03), and ensure empty/error composition helpers and translation keys support short-message + primary-action + recovery-link behavior (D-04). Do not introduce sidebar-only navigation or animation-system expansion.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Shared primitives, shell, and i18n contracts encode all four locked decisions and are ready for route-level rollout.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic UI-07 contract validation anchors</name>
  <read_first>
    - scripts/validate-ui-guidance.js
    - src/components/ui/primitives.tsx
    - src/components/public-shell.tsx
    - .planning/phases/33-global-ux-consistency/33-RESEARCH.md
  </read_first>
  <files>scripts/validate-ui-guidance.js</files>
  <acceptance_criteria>
    - Validator fails when action hierarchy tokens drift from D-02 mapping.
    - Validator fails when empty/error composition contract for D-04 is missing.
    - Validator asserts shell-level hybrid navigation anchors for D-03 and compact density markers for D-01.
  </acceptance_criteria>
  <action>Extend `scripts/validate-ui-guidance.js` with Phase 33 anchors that enforce D-01 through D-04 at shared-component level. Keep checks static/deterministic (no flaky UI snapshots), and ensure command runtime remains suitable for per-task sampling.</action>
  <verify>
    <automated>node scripts/validate-ui-guidance.js && npm run typecheck</automated>
  </verify>
  <done>Automated validator coverage reliably detects regressions against the shared UX consistency contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> navigation/actions | User-triggered navigation must not bypass authenticated route guards |
| translation/state copy -> rendered UI | Localized text must render as text-only without unsafe HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | E | layout/shell navigation | mitigate | Keep existing route auth/permission checks unchanged; this plan only changes presentation contracts. |
| T-33-02 | T | shared action links | mitigate | Reuse framework `Link`/safe href patterns and avoid client-only authorization assumptions in action hierarchy refactors. |
| T-33-03 | I | empty/error copy rendering | mitigate | Render messages via React text nodes and disallow unsafe HTML insertion in primitives. |
</threat_model>

<verification>
Run:
- `node scripts/validate-ui-guidance.js`
- `npm run typecheck`
</verification>

<success_criteria>
- D-01 through D-04 are codified in shared UI contracts and validated automatically.
- Phase 33 rollout plans can consume one consistent primitive/shell system with no ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/33-global-ux-consistency/33-01-SUMMARY.md`
</output>

