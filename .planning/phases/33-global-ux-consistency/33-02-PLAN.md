---
phase: 33-global-ux-consistency
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/app/page.tsx
  - src/app/auth/page.tsx
  - src/app/reporting/page.tsx
  - src/app/reporting/[targetId]/page.tsx
  - src/app/reporting/executive/page.tsx
  - src/app/settings/settings-page.tsx
  - src/app/admin/trust-lists/page.tsx
  - src/app/admin/certificates/page.tsx
  - src/i18n/index.ts
  - scripts/validate-reporting.js
  - scripts/validate-ui-guidance.js
autonomous: true
requirements: [UI-07]
must_haves:
  truths:
    - "Public, reporting, settings, and admin screens feel like one product with the same visual discipline."
    - "All major surfaces use the same button/action hierarchy without local exceptions."
    - "Each area has predictable hybrid navigation: global top nav plus local area sub-navigation."
    - "Empty and error states follow one compact, recovery-oriented pattern everywhere in scope."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Public entry aligned to global consistency contracts"
    - path: "src/app/auth/page.tsx"
      provides: "Auth entry with consistent action and spacing hierarchy"
    - path: "src/app/reporting/page.tsx"
      provides: "Reporting list surface aligned to global UX contract"
    - path: "src/app/settings/settings-page.tsx"
      provides: "Settings local sub-navigation and action hierarchy parity"
    - path: "src/app/admin/trust-lists/page.tsx"
      provides: "Trust-list operations with shared empty/error and action patterns"
    - path: "src/app/admin/certificates/page.tsx"
      provides: "Certificate inventory surface with unified compact density and action hierarchy"
  key_links:
    - from: "src/app/*/page.tsx"
      to: "src/components/ui/primitives.tsx"
      via: "PageShell/PageHeader/ActionGroup/ActionLink/EmptyState/Notice usage"
      pattern: "from .*components/ui/primitives"
    - from: "src/app/settings/settings-page.tsx"
      to: "src/components/public-shell.tsx"
      via: "hybrid navigation parity conventions"
      pattern: "nav|tab"
    - from: "scripts/validate-reporting.js"
      to: "src/app/reporting/*.tsx"
      via: "reporting-surface consistency anchors"
      pattern: "ActionLink|EmptyState|PageShell"
---

<objective>
Apply the shared UX consistency contracts across all major route families in scope and enforce UI-07 as a cross-surface behavior.

Purpose: Deliver D-01 through D-04 end-to-end across public, reporting, settings, and admin surfaces without exceptions.
Output: Refactored major pages plus route-level validator anchors proving uniform action hierarchy, density, hybrid navigation, and empty/error patterns.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-global-ux-consistency/33-CONTEXT.md
@.planning/phases/33-global-ux-consistency/33-RESEARCH.md
@.planning/phases/33-global-ux-consistency/33-01-PLAN.md
@.planning/phases/27-public-shell-and-identity/27-01-SUMMARY.md
@.planning/phases/28-settings-and-administration/28-01-SUMMARY.md
@.planning/phases/31-dashboard-and-reporting/31-02-SUMMARY.md
@src/app/page.tsx
@src/app/auth/page.tsx
@src/app/reporting/page.tsx
@src/app/reporting/[targetId]/page.tsx
@src/app/reporting/executive/page.tsx
@src/app/settings/settings-page.tsx
@src/app/admin/trust-lists/page.tsx
@src/app/admin/certificates/page.tsx

<interfaces>
From src/components/ui/primitives.tsx:
```typescript
export function PageShell({ children }: { children: ReactNode }): ReactElement;
export function PageHeader(...): ReactElement;
export function ActionGroup({ children }: { children: ReactNode }): ReactElement;
export function ActionLink({ href, children, tone = "secondary" }: { href: string; children: ReactNode; tone?: "primary" | "secondary" | "context" }): ReactElement;
export function EmptyState({ title, children }: { title: string; children: ReactNode }): ReactElement;
export function Notice(...): ReactElement;
```

From src/app/settings/settings-page.tsx:
```typescript
function getSavedNoticeKey(saved: string | null): string | null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Roll out compact density and action hierarchy across major surfaces</name>
  <read_first>
    - .planning/phases/33-global-ux-consistency/33-01-PLAN.md
    - src/app/page.tsx
    - src/app/auth/page.tsx
    - src/app/reporting/page.tsx
    - src/app/reporting/[targetId]/page.tsx
    - src/app/reporting/executive/page.tsx
    - src/app/settings/settings-page.tsx
    - src/app/admin/trust-lists/page.tsx
    - src/app/admin/certificates/page.tsx
  </read_first>
  <files>src/app/page.tsx, src/app/auth/page.tsx, src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx, src/app/reporting/executive/page.tsx, src/app/settings/settings-page.tsx, src/app/admin/trust-lists/page.tsx, src/app/admin/certificates/page.tsx, src/i18n/index.ts</files>
  <acceptance_criteria>
    - D-01: all listed routes follow compact shared spacing/density conventions and remove ad-hoc local spacing drift.
    - D-02: action hierarchy on every listed route is primary solid, secondary outline, tertiary link/context with no conflicting local button styles.
    - Existing auth/authorization behavior remains unchanged for protected routes.
  </acceptance_criteria>
  <action>Refactor each in-scope route family to consume the shared contracts from Plan 33-01 and remove residual local style/action islands. Keep behavior intact (no route/access changes), but normalize visual/action composition so primary actions are obvious, secondary actions remain supportive, and tertiary actions stay contextual links per D-02 while maintaining compact density per D-01.</action>
  <behavior>
    - Test 1: validator anchors fail before rollout if a route uses old non-compliant action hierarchy markers.
    - Test 2: validator anchors pass after rollout for each major route family in scope.
  </behavior>
  <verify>
    <automated>npm run typecheck && node scripts/validate-ui-guidance.js</automated>
  </verify>
  <done>Public, reporting, settings, and admin pages render with one compact visual/action language and no route-level hierarchy drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize hybrid navigation and empty/error composition across route families</name>
  <read_first>
    - src/components/public-shell.tsx
    - src/app/layout.tsx
    - src/app/settings/settings-page.tsx
    - src/app/admin/trust-lists/page.tsx
    - src/app/admin/certificates/page.tsx
    - src/app/reporting/page.tsx
  </read_first>
  <files>src/app/page.tsx, src/app/auth/page.tsx, src/app/reporting/page.tsx, src/app/reporting/[targetId]/page.tsx, src/app/reporting/executive/page.tsx, src/app/settings/settings-page.tsx, src/app/admin/trust-lists/page.tsx, src/app/admin/certificates/page.tsx, src/i18n/index.ts, scripts/validate-reporting.js, scripts/validate-ui-guidance.js</files>
  <acceptance_criteria>
    - D-03: each area exposes predictable local sub-navigation under the existing global top navigation model.
    - D-04: empty/error states on in-scope routes consistently use short message + primary action + recovery link pattern.
    - Validator coverage fails on regressions in hybrid nav anchors or empty/error composition.
  </acceptance_criteria>
  <action>Normalize local sub-navigation affordances per area (reporting/settings/admin/public entry points) under existing global top navigation and align state messaging on all listed surfaces to the D-04 composition contract. Update/extend `scripts/validate-reporting.js` and `scripts/validate-ui-guidance.js` so these route-level contracts are enforced by automated checks.</action>
  <behavior>
    - Test 1: nav consistency anchors detect absence of local sub-navigation in area pages where applicable.
    - Test 2: empty/error anchors detect missing primary action and recovery link on any in-scope surface.
  </behavior>
  <verify>
    <automated>node scripts/validate-reporting.js && node scripts/validate-ui-guidance.js && npm run validate</automated>
  </verify>
  <done>Hybrid navigation and empty/error behavior are consistent and automatically guarded across all major routes in phase scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> protected route actions | UX refactors must not weaken existing permission or auth gates |
| ui messaging -> user decisions | Consistent error/recovery links must remain safe and scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | E | reporting/settings/admin pages | mitigate | Preserve existing `assertAuthenticated` and permission guard paths while refactoring UI composition only. |
| T-33-05 | T | action/recovery links | mitigate | Keep links bound to existing trusted routes/handlers; no client-side privilege assumptions introduced by hierarchy changes. |
| T-33-06 | R | route-level UX regressions | mitigate | Add deterministic validator anchors in `validate-ui-guidance` and `validate-reporting` to provide auditable regression detection. |
</threat_model>

<verification>
Run:
- `npm run typecheck`
- `node scripts/validate-reporting.js`
- `node scripts/validate-ui-guidance.js`
- `npm run validate`
</verification>

<success_criteria>
- UI-07 is met across all major route families with one modern/consistent language and navigation pattern.
- D-01 through D-04 are visible in route behavior, not only shared primitives.
- Consistency regressions are caught automatically by validator anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/33-global-ux-consistency/33-02-SUMMARY.md`
</output>

