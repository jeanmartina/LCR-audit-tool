---
phase: 29-trust-lists-and-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/app/admin/trust-lists/page.tsx
  - src/app/admin/trust-lists/trust-list-diagnostics-panel.tsx
  - src/app/admin/trust-lists/[sourceId]/page.tsx
  - src/i18n/index.ts
  - scripts/validate-trust-list-operator-ux.js
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "The main trust-list inventory stays compact and shows source state, last success/failure, and open-details as the primary action."
    - "Hierarchy is visible in the compact card, grouped list, and detail page using badge, indentation, and detail disclosure."
    - "The detail page exposes fetch, XML parse, XMLDSig, projection/import, and hierarchy failure layers explicitly, plus archive and permanent delete controls."
  artifacts:
    - path: "src/app/admin/trust-lists/trust-list-diagnostics-panel.tsx"
      provides: "Reusable compact/detail trust-list diagnostics panel"
    - path: "src/app/admin/trust-lists/page.tsx"
      provides: "Compact trust-list inventory entry point"
    - path: "src/app/admin/trust-lists/[sourceId]/page.tsx"
      provides: "Source detail and diagnostics page"
    - path: "src/i18n/index.ts"
      provides: "Trust-list diagnostics labels and action copy"
    - path: "scripts/validate-trust-list-operator-ux.js"
      provides: "Operator UX validator for compact and detail trust-list surfaces"
  key_links:
    - from: "src/app/admin/trust-lists/page.tsx"
      to: "src/app/admin/trust-lists/[sourceId]/page.tsx"
      via: "open-details navigation"
      pattern: '/admin/trust-lists/.+'
    - from: "src/app/admin/trust-lists/page.tsx"
      to: "src/app/admin/trust-lists/trust-list-diagnostics-panel.tsx"
      via: "shared compact diagnostics panel"
      pattern: 'TrustListDiagnosticsPanel'
    - from: "scripts/validate-trust-list-operator-ux.js"
      to: "src/app/admin/trust-lists/[sourceId]/page.tsx"
      via: "detail-view validation anchors"
      pattern: 'download/fetch|XML/parse|XMLDSig/assinatura|projection/import|hierarquia/LOTL'
---

<objective>
Turn the trust-list operator surface into a compact inventory with a quick detail bridge and a fully explained detail page.

Purpose: the operator must be able to scan state quickly, open details without hunting, and understand exactly which technical layer failed.
Output: a compact hierarchy-aware list, a reusable diagnostics panel, a dedicated source detail page, and updated operator-UX validation for the new experience.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/.planning/phases/29-trust-lists-and-diagnostics/29-CONTEXT.md
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/.planning/phases/29-trust-lists-and-diagnostics/29-RESEARCH.md
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/.planning/phases/29-trust-lists-and-diagnostics/29-VALIDATION.md
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/admin/trust-lists/page.tsx
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/admin/trust-lists/trust-list-source-wizard.tsx
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/trust-lists/admin.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/trust-lists/sync.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/i18n/index.ts
</context>

<interfaces>
From `src/trust-lists/admin.ts`:
```ts
export interface TrustListProjectionCounts {
  imported: number;
  updated: number;
  skippedUnchanged: number;
  skippedDuplicate: number;
  failed: number;
}

export interface TrustListSourceSummary {
  source: TrustListSourceRecord;
  lastSnapshot: TrustListSnapshotRecord | null;
  lastRun: TrustListSyncRunRecord | null;
  lastSuccess: TrustListSyncRunRecord | null;
  lastFailure: TrustListSyncRunRecord | null;
  projectionCounts: TrustListProjectionCounts;
  latestProjectionFailureReason: string | null;
  latestRecovery: TrustListRecoveryGuidance | null;
}
```

From `src/trust-lists/sync.ts`:
```ts
export function getTrustListRecoveryGuidance(reason?: string | null): TrustListRecoveryGuidance | null;
export async function previewTrustListXmlSource(url: string): Promise<TrustListSourcePreviewResult>;
export async function syncTrustListSource(source: TrustListSourceRecord): Promise<TrustListSyncSummary>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 2: Build the compact trust-list diagnostics panel and detail page</name>
  <files>src/app/admin/trust-lists/page.tsx, src/app/admin/trust-lists/trust-list-diagnostics-panel.tsx, src/app/admin/trust-lists/[sourceId]/page.tsx, src/i18n/index.ts, scripts/validate-trust-list-operator-ux.js</files>
  <read_first>
    - src/app/admin/trust-lists/page.tsx
    - src/trust-lists/admin.ts
    - src/trust-lists/sync.ts
    - src/i18n/index.ts
    - scripts/validate-trust-list-operator-ux.js
  </read_first>
  <needs>The lifecycle helpers and hierarchy fields from task 1.</needs>
  <creates>A compact trust-list list surface, a reusable diagnostics panel, and a dedicated detail page with the full failure breakdown.</creates>
  <has_checkpoint>false</has_checkpoint>
  <action>Extract the current trust-list inventory into `src/app/admin/trust-lists/trust-list-diagnostics-panel.tsx` and make both the list page and the new detail page reuse it. On the compact card, show the source state, the last success/failure, the hierarchy badge/indentation, and a single primary action that opens details. In the detail page, show sections for hierarchy, metadata, recovery guidance, snapshots, sync history, projection counts, and the explicit failure layers `download/fetch`, `XML/parse`, `XMLDSig/assinatura`, `projection/import`, and `hierarquia/LOTL`. Add archive and permanent delete actions in both the compact view and the detail view, but keep the permanent delete visually and semantically distinct from archive. Update `src/i18n/index.ts` with the labels needed for open details, hierarchy, archive, delete, and the failure-layer headings, then extend `scripts/validate-trust-list-operator-ux.js` so it checks for those exact trust-list diagnostics anchors and the new detail route.</action>
  <acceptance_criteria>
    - `src/app/admin/trust-lists/trust-list-diagnostics-panel.tsx` exists and is used by the trust-list list page
    - `src/app/admin/trust-lists/[sourceId]/page.tsx` exists
    - the compact trust-list surface contains `open details`
    - the detail page contains all of `download/fetch`, `XML/parse`, `XMLDSig/assinatura`, `projection/import`, and `hierarquia/LOTL`
    - the compact and detail views both expose archive and permanent delete actions
    - `scripts/validate-trust-list-operator-ux.js` checks the new diagnostics and detail anchors
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-trust-list-operator-ux.js && node scripts/validate-all.js && npm run typecheck</automated>
  </verify>
  <done>The trust-list operator surface is compact at a glance, opens details directly, and explains failures in layered technical detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> trust-list diagnostics routes | Untrusted path parameters select which source detail is shown. |
| detail page -> recovery guidance | Failure summaries must remain read-only and not become scriptable state. |
| archive/delete controls -> source history | Archive should preserve history, while permanent delete must remain guarded and explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | T | `src/app/admin/trust-lists/[sourceId]/page.tsx` | mitigate | Validate path parameters server-side and only render sources the operator is allowed to manage. |
| T-29-05 | I | `src/app/admin/trust-lists/trust-list-diagnostics-panel.tsx` | mitigate | Render failure layers and recovery guidance as plain text, not HTML, and keep the detail bridge deterministic. |
| T-29-06 | D | `src/app/admin/trust-lists/page.tsx` | mitigate | Keep archive and delete actions separate so a safe archive path always exists even when permanent removal is blocked. |
</threat_model>

<verification>
- `node scripts/validate-trust-list-operator-ux.js` passes with the new compact and detail trust-list anchors.
- `node scripts/validate-all.js` passes after the diagnostics panel and detail route changes.
- `npm run typecheck` passes after the trust-list UI refactor.
</verification>

<success_criteria>
- The compact inventory is readable and shows the required summary fields.
- Every trust-list source has a direct detail route with full failure-layer visibility.
- Archive and permanent delete are both available, but not visually conflated.
</success_criteria>

<output>
After completion, create `.planning/phases/29-trust-lists-and-diagnostics/29-02-SUMMARY.md`
</output>
