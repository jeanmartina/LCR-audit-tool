---
phase: 30-import-review-and-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inventory/certificate-admin.ts
  - src/storage/runtime-store.ts
  - src/app/api/admin/certificates/import/preview/route.ts
  - src/app/api/admin/certificates/import/route.ts
  - src/app/api/admin/certificates/import-zip/route.ts
  - src/app/api/admin/trust-lists/[sourceId]/sync/route.ts
  - scripts/validate-import-review-foundation.js
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Every import origin (single, ZIP, trust-list-derived) can produce a server-canonical review snapshot before save (per D-05)."
    - "Final save always revalidates on the server and rejects stale/tampered review payloads (per D-03)."
    - "Divergent edited values require mandatory justification before final save (per D-02)."
  artifacts:
    - path: "src/inventory/certificate-admin.ts"
      provides: "Shared review decision contract and final-save revalidation helpers"
    - path: "src/storage/runtime-store.ts"
      provides: "Review snapshot persistence and non-accepted outcome audit records"
    - path: "src/app/api/admin/certificates/import/route.ts"
      provides: "Server-authoritative final save with divergence enforcement"
    - path: "scripts/validate-import-review-foundation.js"
      provides: "Foundation validation for review contract and safety checks"
  key_links:
    - from: "src/app/api/admin/certificates/import/preview/route.ts"
      to: "src/inventory/certificate-admin.ts"
      via: "shared review snapshot creation"
      pattern: "create.*review|preview.*review"
    - from: "src/app/api/admin/certificates/import/route.ts"
      to: "src/storage/runtime-store.ts"
      via: "persist review outcomes and provenance audit"
      pattern: "record.*review|recordCertificateChangeEvent"
    - from: "src/app/api/admin/trust-lists/[sourceId]/sync/route.ts"
      to: "src/inventory/certificate-admin.ts"
      via: "trust-list origin uses same review contract"
      pattern: "origin|trust-list|review"
---

<objective>
Create the shared review-before-save safety foundation so all import origins follow one server-authoritative contract.

Purpose: UI-04 requires policy consistency, revalidation at final save, and auditable non-accepted outcomes across single, ZIP, and trust-list-derived imports.
Output: shared review snapshot/revalidation helpers, route-level enforcement, and foundation validator coverage.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-import-review-and-safety/30-CONTEXT.md
@.planning/phases/30-import-review-and-safety/30-RESEARCH.md
@.planning/phases/30-import-review-and-safety/30-UI-SPEC.md
@.planning/phases/30-import-review-and-safety/30-VALIDATION.md
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/inventory/certificate-admin.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/storage/runtime-store.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/api/admin/certificates/import/preview/route.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/api/admin/certificates/import/route.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/api/admin/certificates/import-zip/route.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/api/admin/trust-lists/[sourceId]/sync/route.ts
</context>

<interfaces>
From `src/inventory/certificate-admin.ts`:
```ts
export interface CertificateInput;
export interface CertificateImportPreview;
export async function previewCertificateImport(...): Promise<CertificateImportPreview>;
export async function importCertificate(...);
export async function importCertificateZip(...): Promise<ImportRunSummary>;
```

From `src/storage/runtime-store.ts`:
```ts
export interface CertificateImportRunRecord;
export interface CertificateImportItemRecord;
export interface CertificateChangeEventRecord;
export async function createCertificateImportRun(...);
export async function recordCertificateImportItem(...);
export async function recordCertificateChangeEvent(...);
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement shared review contract and server-authoritative revalidation (D-01, D-02, D-03, D-04, D-05)</name>
  <files>src/inventory/certificate-admin.ts, src/storage/runtime-store.ts, src/app/api/admin/certificates/import/preview/route.ts, src/app/api/admin/certificates/import/route.ts, src/app/api/admin/certificates/import-zip/route.ts, src/app/api/admin/trust-lists/[sourceId]/sync/route.ts, scripts/validate-import-review-foundation.js</files>
  <read_first>
    - src/inventory/certificate-admin.ts
    - src/storage/runtime-store.ts
    - src/app/api/admin/certificates/import/preview/route.ts
    - src/app/api/admin/certificates/import/route.ts
    - src/app/api/admin/certificates/import-zip/route.ts
    - src/app/api/admin/trust-lists/[sourceId]/sync/route.ts
  </read_first>
  <behavior>
    - Test 1: final save rejects a review payload when server recomputation detects mismatch.
    - Test 2: edited values that diverge from derived values are rejected unless a justification exists.
    - Test 3: review outcomes `ignore`, `reject`, and `duplicate` are recorded in audit/provenance but do not create active certificates.
    - Test 4: single, ZIP, and trust-list origins all use the same review decision model and validation path.
  </behavior>
  <needs>Current preview/import and trust-list sync routes plus runtime import/audit persistence.</needs>
  <creates>Canonical review snapshot contract, divergence enforcement, revalidation-at-save guardrails, and outcome audit persistence.</creates>
  <has_checkpoint>false</has_checkpoint>
  <action>Create a shared review DTO and helper layer in `certificate-admin.ts` that normalizes decisions across all origins (`accept`, `edit`, `ignore`, `reject`, `duplicate`, `pending`) per D-05. Update preview and final-save routes so commit recomputes server-side values from raw/staged inputs and rejects stale/tampered payloads per D-03. Enforce mandatory divergence justification in final-save validation per D-02. Persist non-accepted outcomes and reasons as review/provenance history records without creating active certificate rows per D-04. Add `scripts/validate-import-review-foundation.js` to fail if shared contract symbols, divergence checks, and non-accepted outcome persistence anchors are missing.</action>
  <acceptance_criteria>
    - A shared review decision contract is defined once and used by single, ZIP, and trust-list-origin routes
    - final save path explicitly recomputes and revalidates server-side before mutation
    - divergence requires a non-empty justification field
    - `ignore|reject|duplicate` outcomes are persisted in audit/provenance and skipped for active-certificate creation
    - `scripts/validate-import-review-foundation.js` exists and checks for the required foundation anchors
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-import-review-foundation.js && npm run typecheck</automated>
  </verify>
  <done>The server can enforce the complete review-before-save safety policy for all import origins independent of client behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> preview/save APIs | Client can tamper with staged review payloads and decisions. |
| trust-list sync trigger -> import pipeline | Automated sources can bypass manual review policy if origin parity is not enforced. |
| review decisions -> persistence | Non-accepted outcomes can be incorrectly activated if decision handling is weak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | T | `src/app/api/admin/certificates/import/route.ts` | mitigate | Recompute canonical values server-side and reject mismatches between staged review and recomputed data. |
| T-30-02 | E | `src/app/api/admin/trust-lists/[sourceId]/sync/route.ts` | mitigate | Route trust-list candidates through the same review authorization and decision checks as other origins. |
| T-30-03 | R | `src/storage/runtime-store.ts` | mitigate | Persist explicit decision/reason provenance for `ignore|reject|duplicate` to prevent non-repudiation gaps. |
</threat_model>

<verification>
- `node scripts/validate-import-review-foundation.js` passes.
- `npm run typecheck` passes with updated route and helper contracts.
- `npm run quality` remains green after foundation changes.
</verification>

<success_criteria>
- UI-04 foundation behavior is enforceable server-side before any review UI can claim completion.
- Locked context decisions D-01 through D-05 are represented in executable backend checks.
</success_criteria>

<output>
After completion, create `.planning/phases/30-import-review-and-safety/30-01-SUMMARY.md`
</output>
