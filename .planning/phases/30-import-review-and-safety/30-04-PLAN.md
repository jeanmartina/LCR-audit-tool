---
phase: 30-import-review-and-safety
plan: 04
type: execute
gap_closure: true
wave: 4
depends_on:
  - 30-03
files_modified:
  - src/app/admin/trust-lists/trust-list-source-wizard.tsx
  - src/app/admin/trust-lists/page.tsx
  - src/app/api/admin/trust-lists/[sourceId]/sync/route.ts
  - src/trust-lists/admin.ts
  - src/i18n/index.ts
  - scripts/validate-trust-list-review-ui-gap.js
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Trust-list import preview transitions into an operator-facing per-candidate review surface before save."
    - "Operators can assign candidate decisions in the UI (`accept|edit|ignore|reject|duplicate|pending`) without authoring raw JSON."
    - "Trust-list review submissions are posted as structured candidate decisions from UI state to sync save."
  artifacts:
    - path: "src/app/admin/trust-lists/trust-list-source-wizard.tsx"
      provides: "Candidate review UI and local decision state derived from trust-list preview payload."
    - path: "src/app/api/admin/trust-lists/[sourceId]/sync/route.ts"
      provides: "Structured review-save endpoint consumption from UI-generated candidate decisions."
    - path: "scripts/validate-trust-list-review-ui-gap.js"
      provides: "Regression guard that fails if manual `reviewPayload` JSON textarea path returns."
  key_links:
    - from: "src/app/admin/trust-lists/trust-list-source-wizard.tsx"
      to: "src/app/api/admin/trust-lists/[sourceId]/sync/route.ts"
      via: "preview -> review UI state -> sync save submit"
      pattern: "candidateDecisions"
    - from: "src/trust-lists/admin.ts"
      to: "src/app/admin/trust-lists/trust-list-source-wizard.tsx"
      via: "typed candidate/review DTOs for trust-list review rendering"
      pattern: "TrustList.*Review|candidate"
---

<objective>
Close the remaining Phase 30 verification blocker by replacing manual trust-list `reviewPayload` authoring with a real per-candidate review-before-save flow.

Purpose: UI-04 is still blocked because trust-list origin lacks the same operator review UX already delivered for single and ZIP imports.
Output: trust-list preview-to-review-to-save UI wiring with structured candidate decisions and no manual JSON dependency.
</objective>

<execution_context>
@/home/jeanmartina/.codex/get-shit-done/workflows/execute-plan.md
@/home/jeanmartina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-import-review-and-safety/30-CONTEXT.md
@.planning/phases/30-import-review-and-safety/30-UI-SPEC.md
@.planning/phases/30-import-review-and-safety/30-VERIFICATION.md
@.planning/phases/30-import-review-and-safety/30-03-SUMMARY.md
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/admin/trust-lists/trust-list-source-wizard.tsx
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/app/api/admin/trust-lists/[sourceId]/sync/route.ts
@/home/jeanmartina/orca/workspaces/LCR-audit-tool/Milestone-1.3/src/trust-lists/admin.ts

<interfaces>
From src/app/api/admin/trust-lists/[sourceId]/sync/route.ts:
```ts
// Sync save requires review decisions for candidates.
// Request body includes a structured review payload consumed by trust-list sync.
```

From src/app/admin/trust-lists/trust-list-source-wizard.tsx:
```ts
// Existing wizard has preview and save interactions but currently relies on manual reviewPayload JSON entry.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace manual trust-list review JSON entry with per-candidate review UI state (per D-05)</name>
  <files>src/app/admin/trust-lists/trust-list-source-wizard.tsx, src/trust-lists/admin.ts, src/i18n/index.ts</files>
  <behavior>
    - Test 1: previewed trust-list candidates render in a review list/table with deterministic row identity.
    - Test 2: each candidate exposes `accept|edit|ignore|reject|duplicate|pending` decision controls in UI.
    - Test 3: UI no longer requires raw `reviewPayload` JSON authoring to proceed with review save.
  </behavior>
  <action>Refactor the wizard to hydrate candidate review state directly from preview results and render an explicit per-candidate review surface instead of a freeform `reviewPayload` textarea. Add typed trust-list review DTOs in `src/trust-lists/admin.ts` and use them in wizard state and submit shaping. Add i18n copy for trust-list review controls and row-level validation errors. Keep decision vocabulary exactly aligned with existing shared review contract.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Trust-list wizard displays real candidate review controls and no longer depends on manual JSON payload entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire trust-list review UI preview->save handoff through structured candidate decisions (per D-05)</name>
  <files>src/app/admin/trust-lists/trust-list-source-wizard.tsx, src/app/admin/trust-lists/page.tsx, src/app/api/admin/trust-lists/[sourceId]/sync/route.ts, scripts/validate-trust-list-review-ui-gap.js</files>
  <behavior>
    - Test 1: saving from trust-list review submits structured `candidateDecisions` built from UI state.
    - Test 2: route handles UI review payload shape without manual JSON parsing requirements in the operator flow.
    - Test 3: guard script fails when manual textarea-based `reviewPayload` authoring path is reintroduced.
  </behavior>
  <action>Connect wizard preview state to final sync save submission so payload is generated programmatically from candidate decision state and posted to the trust-list sync route. Ensure page-level flow transitions preview -> review -> save without leaving candidate decisions disconnected. Add `scripts/validate-trust-list-review-ui-gap.js` to assert presence of candidate review rendering and structured submit path, and assert absence of raw JSON review authoring anchors.</action>
  <verify>
    <automated>node scripts/validate-trust-list-review-ui-gap.js && npm run quality</automated>
  </verify>
  <done>The unresolved trust-list UI gap is closed: operators can complete review-before-save using per-candidate controls fully wired to sync save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trust-list preview response -> browser review state | Candidate data crosses into mutable client state and must map deterministically to save payload rows. |
| browser review state -> trust-list sync save route | Decision state can be incomplete or tampered with before submit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | T | `src/app/admin/trust-lists/trust-list-source-wizard.tsx` | mitigate | Build save payload from typed candidate state with required decision defaults instead of accepting freeform JSON text. |
| T-30-11 | R | `src/app/api/admin/trust-lists/[sourceId]/sync/route.ts` | mitigate | Preserve route-side structured decision validation and reject malformed/missing candidate decision payloads. |
| T-30-12 | D | `scripts/validate-trust-list-review-ui-gap.js` | mitigate | Add regression guard that fails CI when manual JSON-based review authoring path reappears. |
</threat_model>

<verification>
- `npm run typecheck` passes after trust-list review UI contract changes.
- `node scripts/validate-trust-list-review-ui-gap.js` passes and confirms preview->review->save wiring with no manual JSON dependency.
- `npm run quality` remains green with the gap closure applied.
</verification>

<success_criteria>
- Verification gap "trust-list origin lacks real per-candidate review UI wired preview->save without manual JSON authoring" is resolved.
- UI-04 is fully satisfied across single, ZIP, and trust-list flows in operator-facing UX and route wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/30-import-review-and-safety/30-04-SUMMARY.md`
</output>
