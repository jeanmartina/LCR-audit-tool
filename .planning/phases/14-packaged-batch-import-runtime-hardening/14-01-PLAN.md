---
phase: 14-packaged-batch-import-runtime-hardening
plan: 01
type: execute
wave: 1
depends_on: [13-01]
files_modified: [src/inventory/, src/app/api/admin/certificates/, src/storage/runtime-store.ts, Dockerfile, compose.yaml, scripts/, README.md, docs/operators.md, .planning/]
autonomous: true
requirements: [ADM-02]
must_haves:
  truths:
    - "The shipped runtime must no longer depend on an undeclared host `unzip` binary."
    - "Batch import proof must happen in the packaged compose stack, not only from source execution."
    - "Readable archives keep partial-success semantics; corrupt archives fail the import run at the archive level."
    - "Supported certificate files inside `.zip` remain `.pem`, `.crt`, and `.cer` with PEM and DER encodings."
    - "No hard archive-size limit is introduced unless the implementation explicitly enforces one."
  artifacts:
    - src/inventory/
    - src/app/api/admin/certificates/
    - src/storage/runtime-store.ts
    - Dockerfile
    - compose.yaml
    - scripts/
    - README.md
    - docs/operators.md
  key_links:
    - .planning/phases/14-packaged-batch-import-runtime-hardening/14-CONTEXT.md
    - .planning/phases/14-packaged-batch-import-runtime-hardening/14-RESEARCH.md
    - .planning/v1.1-MILESTONE-AUDIT.md
    - .planning/REQUIREMENTS.md
---
<objective>
Make `.zip` certificate onboarding work inside the shipped compose/runtime stack and prove that packaged batch import projects derived CRLs into persisted reporting data.
Purpose: The milestone audit found that the current batch-import implementation depends on `unzip`, but the shipped runtime image does not provide it. Phase 14 closes that packaged-runtime gap without changing the certificate-first product model.
Output: self-contained batch import in the packaged runtime, explicit archive-level failure handling, compose-based proof coverage, and updated operator documentation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Replace external archive extraction with self-contained runtime extraction</name>
  <files>src/inventory/certificate-admin.ts, src/app/api/admin/certificates/import-zip/route.ts, package.json, scripts/</files>
  <read_first>.planning/phases/14-packaged-batch-import-runtime-hardening/14-CONTEXT.md, .planning/phases/14-packaged-batch-import-runtime-hardening/14-RESEARCH.md, src/inventory/certificate-admin.ts, src/app/api/admin/certificates/import-zip/route.ts</read_first>
  <action>Remove the external `unzip` shell dependency from the batch-import path and replace it with a self-contained in-process extraction flow that works in the packaged runtime. Keep the existing certificate-first import semantics while ensuring `.pem`, `.crt`, and `.cer` files can be processed from both PEM and DER encodings.</action>
  <verify>Add validation that proves the batch-import path no longer shells out to `unzip`, and that the supported file types/encodings remain accepted by the runtime import code.</verify>
  <acceptance_criteria>
    - The packaged application no longer requires an undeclared OS-level `unzip` binary for batch import.
    - The batch import path accepts `.pem`, `.crt`, and `.cer` files from `.zip` archives.
    - PEM and DER certificate encodings both normalize into the same import pipeline.
    - The fix preserves fingerprint-based dedup and derived-CRL projection behavior.
  </acceptance_criteria>
  <done>The shipped runtime can extract and process certificate archives without relying on host tooling.</done>
</task>

<task type="auto">
  <name>Task 2: Harden batch-import runtime behavior and archive-level failure reporting</name>
  <files>src/inventory/certificate-admin.ts, src/storage/runtime-store.ts, src/app/admin/, src/app/api/admin/certificates/</files>
  <read_first>.planning/phases/14-packaged-batch-import-runtime-hardening/14-CONTEXT.md, src/inventory/certificate-admin.ts, src/storage/runtime-store.ts</read_first>
  <action>Make the packaged batch-import path distinguish clearly between readable archives with item-level partial success and corrupt/unreadable archives that fail at the archive level. Persist import-run outcomes in a way that leaves operators with a concrete top-level archive error for unreadable uploads and item-specific reasons for readable-archive failures.</action>
  <verify>Validation asserts that corrupt archives create a failed import run without per-item processing, while readable archives still produce imported/updated/ignored/invalid summaries with operator-readable reasons.</verify>
  <acceptance_criteria>
    - Corrupt/unreadable archives do not masquerade as partial-success batches.
    - Readable archives retain item-level partial success semantics.
    - Operators can distinguish top-level archive failure from certificate-level failure.
    - The persisted import-run model remains usable by admin/reporting surfaces.
  </acceptance_criteria>
  <done>The batch-import runtime contract is operationally clear and auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the packaged compose path and update the runtime contract docs</name>
  <files>Dockerfile, compose.yaml, scripts/, README.md, docs/operators.md, .planning/</files>
  <read_first>.planning/phases/14-packaged-batch-import-runtime-hardening/14-CONTEXT.md, .planning/v1.1-MILESTONE-AUDIT.md, Dockerfile, compose.yaml, README.md, docs/operators.md</read_first>
  <action>Add a reproducible packaged-stack verification path that proves batch import works inside the compose deployment and that derived CRLs become visible to persisted reporting data. Update the operator docs to describe the supported archive/file formats, the absence of a hard size limit for now, and the compose-based proof path.</action>
  <verify>Validation confirms the packaged proof path exists, is compose-based, and documents supported `.zip` contents, PEM/DER handling, and current runtime-resource-bound limits.</verify>
  <acceptance_criteria>
    - The compose-based stack has an explicit proof path for packaged batch import.
    - The proof path shows import-run success/failure plus reporting-visible derived CRLs.
    - Docs describe supported `.zip` inputs and current archive-size assumptions accurately.
    - The milestone audit can point to packaged-runtime evidence instead of source-only behavior.
  </acceptance_criteria>
  <done>The packaged batch-import gap is closed with auditable evidence and a clear runtime contract.</done>
</task>

</tasks>

<verification>
- [ ] Batch import no longer depends on the external `unzip` binary
- [ ] `.zip` archives containing `.pem`, `.crt`, and `.cer` files are supported
- [ ] PEM and DER certificate encodings are both accepted in the batch path
- [ ] Corrupt archives fail at the archive level with a top-level error
- [ ] Readable archives still support partial success with item-level reasons
- [ ] Derived CRLs from packaged batch import reach persisted reporting-visible state
- [ ] The compose-based proof path exists and is documented
- [ ] Docs state that archive size is not hard-limited yet and is constrained by runtime resources
- [ ] Validation covers runtime extraction, failure semantics, and packaged proof
</verification>

<success_criteria>
- The packaged `.zip` onboarding gap from the milestone audit is closed in the shipped runtime path.
- The app image is self-sufficient for batch import and does not rely on undeclared host binaries.
- Operators get clearer diagnostics for corrupt archives versus readable partial-success batches.
- The phase leaves a concrete compose-based proof path for re-running the milestone audit and satisfying `ADM-02`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-packaged-batch-import-runtime-hardening/14-01-SUMMARY.md`
</output>
